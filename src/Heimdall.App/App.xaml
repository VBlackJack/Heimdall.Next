<Application x:Class="Heimdall.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Heimdall.App.Converters"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Default theme: DraculaPro -->
                <ResourceDictionary Source="Themes/DraculaProTheme.xaml"/>
                <!-- Vector icon geometries -->
                <ResourceDictionary Source="Themes/IconGeometries.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Value converters -->
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <converters:InvertBoolConverter x:Key="InvertBoolConverter"/>
            <converters:ConnectionTypeToBrushConverter x:Key="ConnectionTypeToBrushConverter"/>
            <converters:ConnectionStateToBrushConverter x:Key="ConnectionStateToBrushConverter"/>
            <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
            <converters:PaletteActiveIndicatorConverter x:Key="PaletteActiveIndicatorConverter"/>
            <converters:ServerStatusToColorConverter x:Key="ServerStatusToColorConverter"/>
            <converters:ConnectionTypeToGeometryConverter x:Key="TypeToGeoConverter"/>
            <converters:ConnectionTypeToColorConverter x:Key="TypeToColorConverter"/>
            <converters:ConnectionStateToGeometryConverter x:Key="StateToGeoConverter"/>
            <converters:StringToBrushConverter x:Key="StringToBrushConverter"/>
            <converters:ResourceKeyToBrushConverter x:Key="ResourceKeyToBrushConverter"/>
            <converters:ResourceKeyToGeometryConverter x:Key="ResourceKeyToGeometryConverter"/>
            <converters:BoolToLocalizedKeyConverter x:Key="ShareFolderLabelConverter"
                                                    FalseKey="ToolsShareFolder"
                                                    TrueKey="ToolsStopSharing"/>

            <!-- Toolbar navigation tab style (RadioButton as flat tab) -->
            <Style x:Key="ToolbarTabStyle" TargetType="RadioButton">
                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="TabBorder"
                                    Background="Transparent"
                                    BorderThickness="0,0,0,2"
                                    BorderBrush="Transparent"
                                    Padding="12,8"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="TabBorder" Property="BorderBrush"
                                            Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="Foreground"
                                            Value="{DynamicResource TextPrimaryBrush}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="TabBorder" Property="Background"
                                            Value="{DynamicResource HighlightBrush}"/>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="TabBorder" Property="BorderBrush"
                                            Value="{DynamicResource FocusIndicatorBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
