<Window x:Class="Heimdall.App.MainWindow"
        x:Name="MainWindowRoot"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Heimdall.App.ViewModels"
        xmlns:views="clr-namespace:Heimdall.App.Views"
        xmlns:models="clr-namespace:Heimdall.Core.Models;assembly=Heimdall.Core"
        xmlns:loc="clr-namespace:Heimdall.App.Localization"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        Title="{Binding WindowTitle}"
        Width="1400" Height="900"
        MinWidth="800" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Display"
        Background="{DynamicResource BackgroundBrush}">

    <Window.Resources>
        <!-- Implicit DataTemplates for recursive split pane tree rendering -->
        <DataTemplate DataType="{x:Type models:SessionPaneModel}">
            <views:SessionPaneControl/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type models:SplitContainerModel}">
            <views:SplitContainerControl/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <!-- Toolbar -->
            <RowDefinition x:Name="ToolbarRow" Height="48"/>
            <!-- Main content -->
            <RowDefinition Height="*"/>
            <!-- Status bar -->
            <RowDefinition x:Name="StatusBarRow" Height="36"/>
        </Grid.RowDefinitions>

        <!-- TOOLBAR -->
        <Border Grid.Row="0"
                Panel.ZIndex="100"
                Background="{DynamicResource SurfaceBrush}"
                BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource BorderBrush}">
            <DockPanel Margin="8,0">
                <!-- Left: App title + navigation tabs -->
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <TextBlock x:Name="Mw_AppTitle" Text=""
                               FontSize="{StaticResource FontSizeLarge}" FontWeight="Bold"
                               Foreground="{DynamicResource AccentBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,24,0"/>

                    <RadioButton x:Name="TabSessions"
                                 Content=""
                                 GroupName="NavTabs"
                                 IsChecked="True"
                                 Focusable="True"
                                 Style="{DynamicResource ToolbarTabStyle}"
                                 Margin="0,0,4,0"
                                 AutomationProperties.Name="{loc:Translate A11ySessionsTab}"/>
                    <RadioButton x:Name="TabTunnels"
                                 Content=""
                                 GroupName="NavTabs"
                                 Focusable="True"
                                 Style="{DynamicResource ToolbarTabStyle}"
                                 Margin="0,0,4,0"
                                 AutomationProperties.Name="{loc:Translate A11yTunnelsTab}"/>
                    <RadioButton x:Name="TabScheduled"
                                 Content=""
                                 GroupName="NavTabs"
                                 Focusable="True"
                                 Style="{DynamicResource ToolbarTabStyle}"
                                 Margin="0,0,4,0"
                                 AutomationProperties.Name="{loc:Translate A11yScheduledTab}"/>
                    <RadioButton x:Name="TabTools"
                                 Content=""
                                 GroupName="NavTabs"
                                 Focusable="True"
                                 Style="{DynamicResource ToolbarTabStyle}"
                                 Margin="0,0,4,0"
                                 AutomationProperties.Name="{loc:Translate A11yToolsTab}"/>
                    <RadioButton x:Name="TabSettings"
                                 GroupName="NavTabs"
                                 Focusable="True"
                                 Style="{DynamicResource ToolbarTabStyle}"
                                 Margin="0,0,4,0"
                                 AutomationProperties.Name="{loc:Translate A11ySettingsTab}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="Mw_TabSettingsText" Text=""/>
                            <Ellipse x:Name="Mw_SettingsUnsavedDot"
                                     Width="8" Height="8"
                                     Fill="{DynamicResource WarningBrush}"
                                     Margin="6,0,0,0"
                                     VerticalAlignment="Center"
                                     Visibility="{Binding Settings.IsDirty, Converter={StaticResource BoolToVisibilityConverter}}"
                                     ToolTip=""/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="TabAbout"
                                 Content=""
                                 GroupName="NavTabs"
                                 Focusable="True"
                                 Style="{DynamicResource ToolbarTabStyle}"
                                 AutomationProperties.Name="{loc:Translate A11yAboutTab}"/>
                </StackPanel>

                <!-- Right: Search box + Share Folder -->
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <!-- Share Folder toggle -->
                    <Button x:Name="ShareFolderButton"
                            Click="OnShareFolderClick"
                            Style="{DynamicResource ToolbarGhostButtonStyle}"
                            Padding="6,4" FontSize="{StaticResource FontSizeBody}"
                            Margin="0,0,4,0"
                            VerticalAlignment="Center"
                            AutomationProperties.Name="{loc:Translate A11yShareFolder}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE72D;" FontFamily="Segoe MDL2 Assets"
                                       FontSize="{StaticResource FontSizeBodyLarge}" VerticalAlignment="Center" Margin="0,0,4,0"
                                       AutomationProperties.Name=""/>
                            <TextBlock x:Name="Mw_ShareFolderLabel" Text="" FontSize="{StaticResource FontSizeBody}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <!-- Quick Connect shortcut button -->
                    <Button x:Name="QuickConnectButton"
                            Click="OnQuickConnectButtonClick"
                            Style="{DynamicResource ToolbarGhostButtonStyle}"
                            Padding="8,4" FontSize="{StaticResource FontSizeCaption}"
                            Margin="0,0,8,0"
                            VerticalAlignment="Center"
                            >
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE71E;" FontFamily="Segoe MDL2 Assets"
                                       FontSize="{StaticResource FontSizeBody}" VerticalAlignment="Center" Margin="0,0,6,0"
                                       AutomationProperties.Name=""/>
                            <TextBlock x:Name="Mw_QuickConnectLabel" Text=""
                                       FontSize="{StaticResource FontSizeCaption}" VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Global busy indicator -->
        <ProgressBar Grid.Row="1"
                     VerticalAlignment="Top"
                     Panel.ZIndex="10"
                     Height="2"
                     IsIndeterminate="True"
                     Foreground="{DynamicResource AccentBrush}"
                     Background="Transparent"
                     BorderThickness="0"
                     IsHitTestVisible="False"
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- MAIN CONTENT -->
        <Grid x:Name="MainContentArea" Grid.Row="1"
              ClipToBounds="True">

            <!-- ============================================================ -->
            <!-- SERVERS TAB: TreeView left panel + detail/sessions right      -->
            <!-- ============================================================ -->
            <Grid Visibility="{Binding IsSessionsTabSelected, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <!-- Left: TreeView panel -->
                    <ColumnDefinition x:Name="SessionTreeColumn" Width="260" MinWidth="180" MaxWidth="500"/>
                    <!-- Splitter -->
                    <ColumnDefinition x:Name="SplitterColumn" Width="Auto"/>
                    <!-- Right: Detail / session content -->
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT PANEL: TreeView with filter (only on Servers tab) -->
                <Border x:Name="TreeViewPanel" Grid.Column="0"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderThickness="0,0,1,0"
                        BorderBrush="{DynamicResource BorderBrush}"
                        >
                    <DockPanel>
                        <!-- Sidebar tab selector: Servers / Tools + collapse sidebar button -->
                        <DockPanel DockPanel.Dock="Top" Margin="8,8,8,0">
                            <Button x:Name="ToggleSidebarButton"
                                    DockPanel.Dock="Right"
                                    Content="&#xE89F;"
                                    FontFamily="Segoe MDL2 Assets"
                                    Click="OnToggleSidebarClick"
                                    Style="{DynamicResource SecondaryButtonStyle}"
                                    Padding="6,6" FontSize="{StaticResource FontSizeBody}"
                                    Width="32" Height="32"
                                    Margin="4,0,0,0"/>
                            <UniformGrid Columns="2">
                                <RadioButton x:Name="SidebarTabSessions"
                                             GroupName="SidebarTabs"
                                             IsChecked="{Binding Sidebar.IsToolsTabActive, Converter={StaticResource InvertBoolConverter}, Mode=OneWay, FallbackValue=True}"
                                             Content="{loc:Translate SidebarTabSessions}"
                                             Style="{DynamicResource SidebarTabStyle}"
                                             KeyboardNavigation.TabIndex="0"
                                             Checked="OnSidebarTabSessionsChecked"
                                             AutomationProperties.Name="{loc:Translate A11ySidebarTabSessions}"/>
                                <RadioButton x:Name="SidebarTabTools"
                                             GroupName="SidebarTabs"
                                             IsChecked="{Binding Sidebar.IsToolsTabActive, Mode=OneWay}"
                                             Content="{loc:Translate SidebarTabTools}"
                                             Style="{DynamicResource SidebarTabStyle}"
                                             KeyboardNavigation.TabIndex="1"
                                             Checked="OnSidebarTabToolsChecked"
                                             AutomationProperties.Name="{loc:Translate A11ySidebarTabTools}"/>
                            </UniformGrid>
                        </DockPanel>

                        <!-- Sidebar tab content: Sessions (TreeView) and Tools are mutually exclusive.
                             Placed in a Grid so they both consume the remaining vertical space; only
                             one is visible at a time via the RadioButton IsChecked state. -->
                        <Grid>
                            <!-- ============ SESSIONS TAB CONTENT ============ -->
                            <DockPanel x:Name="SidebarSessionsContent"
                                       Visibility="{Binding IsChecked, ElementName=SidebarTabSessions, Converter={StaticResource BoolToVisibilityConverter}}">
                                <!-- Filter + add/expand/collapse toolbar -->
                                <DockPanel DockPanel.Dock="Top" Margin="8,8,8,4">
                                    <Button x:Name="CollapseAllButton"
                                            DockPanel.Dock="Right"
                                            Content="&#xE70D;"
                                            FontFamily="Segoe MDL2 Assets"
                                            Click="OnCollapseAllClick"
                                            Style="{DynamicResource ToolbarGhostButtonStyle}"
                                            Padding="6,6" FontSize="{StaticResource FontSizeCaption}"
                                            Width="32" Height="32"
                                            Margin="2,0,0,0"/>
                                    <Button x:Name="ExpandAllButton"
                                            DockPanel.Dock="Right"
                                            Content="&#xE70E;"
                                            FontFamily="Segoe MDL2 Assets"
                                            Click="OnExpandAllClick"
                                            Style="{DynamicResource ToolbarGhostButtonStyle}"
                                            Padding="6,6" FontSize="{StaticResource FontSizeCaption}"
                                            Width="32" Height="32"
                                            Margin="2,0,0,0"/>
                                    <Button x:Name="AddButton"
                                            DockPanel.Dock="Right"
                                            Click="OnAddButtonClick"
                                            Style="{DynamicResource ToolbarGhostButtonStyle}"
                                            Padding="6,6" FontSize="{StaticResource FontSizeBody}"
                                            Width="32" Height="32"
                                            Margin="4,0,0,0">
                                        <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" FontSize="{StaticResource FontSizeBody}"
                                                   AutomationProperties.Name=""/>
                                        <Button.ContextMenu>
                                            <ContextMenu x:Name="AddMenu"
                                                         DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                                <MenuItem x:Name="Mw_AddMenuSession" Header="" Command="{Binding ServerList.AddServerCommand}"/>
                                                <MenuItem x:Name="Mw_AddMenuTool" Header="" Click="OnAddToolMenuClick"/>
                                                <Separator/>
                                                <MenuItem x:Name="Mw_AddMenuGateway" Header="" Command="{Binding Settings.AddGatewayCommand}"/>
                                                <MenuItem x:Name="Mw_AddMenuFolder" Header="" Click="OnAddFolderFromMenu"/>
                                            </ContextMenu>
                                        </Button.ContextMenu>
                                    </Button>
                                    <Grid>
                                        <TextBox x:Name="Mw_FilterBox" Text="{Binding ServerList.SearchText, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{DynamicResource SearchTextBoxStyle}"
                                                 Tag="" Padding="10,6,28,6"
                                                 AutomationProperties.Name="{loc:Translate A11ySearchAndFilter}"/>
                                        <Button x:Name="Mw_FilterClearBtn"
                                                Content="&#xE711;"
                                                FontFamily="Segoe MDL2 Assets"
                                                FontSize="{StaticResource FontSizeSmallCaption}"
                                                HorizontalAlignment="Right" VerticalAlignment="Center"
                                                Margin="0,0,4,0"
                                                Padding="4"
                                                Style="{DynamicResource ToolbarGhostButtonStyle}"
                                                MinHeight="0"
                                                Click="OnFilterClearClick"
                                                Visibility="Collapsed"
                                                AutomationProperties.Name="{loc:Translate A11yClearSearchFilter}"/>
                                    </Grid>
                                    <TextBlock x:Name="Mw_FilterResultCount"
                                               FontSize="{StaticResource FontSizeSmallCaption}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               HorizontalAlignment="Right"
                                               Margin="0,2,4,0"
                                               Visibility="Collapsed"/>
                                </DockPanel>

                                <!-- Hierarchical TreeView (fills remaining space) -->
                                <TreeView x:Name="SessionTreeView"
                                  ItemsSource="{Binding ServerList.GroupedServers}"
                                  Style="{DynamicResource ThemedTreeViewStyle}"
                                  Padding="0,4,0,0"
                                  AllowDrop="True"
                                  SelectedItemChanged="OnTreeViewSelectedItemChanged"
                                  MouseDoubleClick="OnTreeViewDoubleClick"
                                  PreviewMouseLeftButtonDown="OnTreeViewDragStart"
                                  PreviewMouseMove="OnTreeViewDragMove"
                                  DragOver="OnTreeViewDragOver"
                                  DragLeave="OnTreeViewDragLeave"
                                  Drop="OnTreeViewDrop"
                                  PreviewMouseRightButtonDown="OnTreeViewPreviewMouseRightButtonDown"
                                  ContextMenuOpening="OnTreeViewContextMenuOpening"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  ScrollViewer.CanContentScroll="True"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.VirtualizationMode="Recycling">
                            <TreeView.Resources>
                                <!-- Folder node template (recursive — unlimited nesting) -->
                                <HierarchicalDataTemplate DataType="{x:Type vm:FolderViewModel}"
                                                          ItemsSource="{Binding Children}">
                                    <Grid Margin="0,2,0,0" ToolTip="{Binding FullPath}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Path Grid.Column="0"
                                              Data="{StaticResource Geo.Tree.Group}"
                                              Fill="{DynamicResource InfoBrush}"
                                              Width="14" Height="14"
                                              Stretch="Uniform"
                                              VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Name}"
                                                   FontSize="{StaticResource FontSizeBody}" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <Border Grid.Column="2" Padding="6,2" CornerRadius="{StaticResource CornerRadiusMd}" Margin="6,0,0,0"
                                                Background="{DynamicResource CardBrush}"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="{Binding ServerCount}" FontSize="{StaticResource FontSizeCaption}"
                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                       HorizontalAlignment="Center"/>
                                        </Border>
                                    </Grid>
                                </HierarchicalDataTemplate>

                                <!-- Server leaf node template -->
                                <DataTemplate DataType="{x:Type vm:ServerItemViewModel}">
                                    <StackPanel Orientation="Horizontal" Margin="0,1"
                                                ToolTip="{Binding DisplayName}">
                                        <!-- Protocol / tool vector icon (resolved by converters).
                                             The ThemeRevision trigger forces re-evaluation on runtime theme swap. -->
                                        <Path Data="{Binding ConnectionType, Converter={StaticResource TypeToGeoConverter}}"
                                              Style="{StaticResource GeoIconSmall}"
                                              VerticalAlignment="Center" Margin="0,0,4,0">
                                            <Path.Fill>
                                                <MultiBinding Converter="{StaticResource TypeToColorConverter}">
                                                    <Binding Path="ConnectionType"/>
                                                    <Binding Path="DataContext.ThemeRevision" ElementName="MainWindowRoot"/>
                                                </MultiBinding>
                                            </Path.Fill>
                                        </Path>
                                        <!-- Status dot -->
                                        <Ellipse Width="7" Height="7" Margin="2,0,8,0"
                                                 VerticalAlignment="Center">
                                            <Ellipse.Fill>
                                                <MultiBinding Converter="{StaticResource ServerStatusToColorConverter}">
                                                    <Binding Path="ConnectionType"/>
                                                    <Binding Path="ConnectionState"/>
                                                    <Binding Path="DataContext.ThemeRevision" ElementName="MainWindowRoot"/>
                                                </MultiBinding>
                                            </Ellipse.Fill>
                                        </Ellipse>
                                        <!-- Server name -->
                                        <TextBlock Text="{Binding DisplayName}" FontSize="{StaticResource FontSizeBody}"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </DataTemplate>
                            </TreeView.Resources>

                            <!-- TreeViewItem container style: expand state bound to ViewModel -->
                            <TreeView.ItemContainerStyle>
                                <Style TargetType="TreeViewItem"
                                       BasedOn="{StaticResource ThemedTreeViewItemStyle}">
                                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                    <Setter Property="Padding" Value="1,1"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                </Style>
                            </TreeView.ItemContainerStyle>

                                </TreeView>
                            </DockPanel>

                            <!-- ============ TOOLS TAB CONTENT ============ -->
                            <DockPanel x:Name="SidebarToolsContent"
                                       Visibility="{Binding IsChecked, ElementName=SidebarTabTools, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Grid DockPanel.Dock="Top" Margin="8,8,8,4">
                                    <TextBox x:Name="Mw_SidebarToolsFilter"
                                             Style="{DynamicResource SearchTextBoxStyle}"
                                             Tag=""
                                             Padding="10,6,28,6"
                                             Text="{Binding Sidebar.FilterText, UpdateSourceTrigger=PropertyChanged}"
                                             AutomationProperties.Name="{loc:Translate A11ySidebarTabTools}"/>
                                </Grid>
                                <TextBlock x:Name="Mw_SidebarToolsContextText"
                                           DockPanel.Dock="Top"
                                           Margin="10,2,10,4"
                                           FontSize="{StaticResource FontSizeSmallCaption}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextTrimming="CharacterEllipsis"
                                           Text="{Binding Sidebar.ContextLabel}"
                                           ToolTip="{Binding Sidebar.ContextTooltip}"/>
                                <TextBlock x:Name="Mw_SidebarToolsNoResults"
                                           DockPanel.Dock="Top"
                                           Margin="10,16,10,0"
                                           FontSize="{StaticResource FontSizeCaption}"
                                           Foreground="{DynamicResource TextDisabledBrush}"
                                           HorizontalAlignment="Center"
                                           Text="{Binding Sidebar.NoResultsText}"
                                           Visibility="{Binding Sidebar.HasNoResults, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <TreeView x:Name="SidebarToolsTreeView"
                                          Style="{DynamicResource ThemedTreeViewStyle}"
                                          Padding="0,2,0,0"
                                          BorderThickness="0"
                                          Background="Transparent"
                                          ItemsSource="{Binding Sidebar.ToolsCategories}"
                                          SelectedItemChanged="OnSidebarToolsSelectedItemChanged"
                                          MouseDoubleClick="OnSidebarToolsDoubleClick"
                                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                          VirtualizingPanel.IsVirtualizing="True"
                                          VirtualizingPanel.VirtualizationMode="Recycling">
                                    <TreeView.Resources>
                                        <!-- Category header: accent dot + name + count badge -->
                                        <HierarchicalDataTemplate DataType="{x:Type vm:SidebarToolCategoryViewModel}"
                                                                  ItemsSource="{Binding Tools}">
                                            <Grid Margin="0,2,4,2" ToolTip="{Binding CategoryName}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Ellipse Grid.Column="0"
                                                         Width="8" Height="8"
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,6,0">
                                                    <Ellipse.Fill>
                                                        <MultiBinding Converter="{StaticResource ResourceKeyToBrushConverter}">
                                                            <Binding Path="BrushKey"/>
                                                            <Binding Path="DataContext.ThemeRevision" ElementName="MainWindowRoot"/>
                                                        </MultiBinding>
                                                    </Ellipse.Fill>
                                                </Ellipse>
                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding CategoryName}"
                                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                                           FontWeight="SemiBold"
                                                           FontSize="{StaticResource FontSizeBody}"
                                                           VerticalAlignment="Center"
                                                           TextTrimming="CharacterEllipsis"
                                                           AutomationProperties.Name="{Binding CategoryName}"/>
                                                <Border Grid.Column="2"
                                                        Background="{DynamicResource CardBrush}"
                                                        CornerRadius="{StaticResource CornerRadiusMd}"
                                                        Padding="6,1" Margin="6,0,0,0"
                                                        VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding VisibleCount}"
                                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                                               FontSize="{StaticResource FontSizeCaption}"
                                                               HorizontalAlignment="Center"/>
                                                </Border>
                                            </Grid>
                                        </HierarchicalDataTemplate>
                                        <!-- Tool leaf: icon + name (compact single-line) -->
                                        <DataTemplate DataType="{x:Type vm:SidebarToolItemViewModel}">
                                            <StackPanel Orientation="Horizontal"
                                                        Margin="0,1"
                                                        Cursor="Hand"
                                                        ToolTip="{Binding Name}">
                                                <Path Width="14" Height="14"
                                                      Stretch="Uniform"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,8,0"
                                                      Data="{Binding IconGeometryKey, Converter={StaticResource ResourceKeyToGeometryConverter}}">
                                                    <Path.Fill>
                                                        <MultiBinding Converter="{StaticResource ResourceKeyToBrushConverter}">
                                                            <Binding Path="BrushKey"/>
                                                            <Binding Path="DataContext.ThemeRevision" ElementName="MainWindowRoot"/>
                                                        </MultiBinding>
                                                    </Path.Fill>
                                                </Path>
                                                <TextBlock Text="{Binding Name}"
                                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                                           FontSize="{StaticResource FontSizeBody}"
                                                           VerticalAlignment="Center"
                                                           TextTrimming="CharacterEllipsis"
                                                           AutomationProperties.Name="{Binding Name}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </TreeView.Resources>
                                    <TreeView.ItemContainerStyle>
                                        <Style TargetType="TreeViewItem"
                                               BasedOn="{StaticResource ThemedTreeViewItemStyle}">
                                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                            <Setter Property="Visibility"
                                                    Value="{Binding IsVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            <Setter Property="Padding" Value="1,1"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        </Style>
                                    </TreeView.ItemContainerStyle>
                                </TreeView>
                            </DockPanel>
                        </Grid>
                    </DockPanel>
                </Border>

                <!-- SPLITTER -->
                <GridSplitter Grid.Column="1"
                              Width="6"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Stretch"
                              Background="{DynamicResource CardBrush}"
                              Cursor="SizeWE"
                              ShowsPreview="True">
                    <GridSplitter.Style>
                        <Style TargetType="GridSplitter">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </GridSplitter.Style>
                    <!-- Visual drag handle (three dots) -->
                    <GridSplitter.Template>
                        <ControlTemplate TargetType="GridSplitter">
                            <Border Background="{TemplateBinding Background}">
                                <TextBlock Text="&#xE784;" FontFamily="Segoe MDL2 Assets"
                                           FontSize="{StaticResource FontSizeSmallCaption}" Foreground="{DynamicResource TextSecondaryBrush}"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           AutomationProperties.Name=""
                                           RenderTransformOrigin="0.5,0.5">
                                    <TextBlock.RenderTransform>
                                        <RotateTransform Angle="90"/>
                                    </TextBlock.RenderTransform>
                                </TextBlock>
                            </Border>
                        </ControlTemplate>
                    </GridSplitter.Template>
                </GridSplitter>

                <!-- Show sidebar button (visible only when sidebar is hidden) -->
                <Button x:Name="ShowSidebarButton"
                        Grid.Column="2"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Content="&#xE76C;"
                        FontFamily="Segoe MDL2 Assets"
                        Click="OnToggleSidebarClick"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        Padding="4,6" FontSize="{StaticResource FontSizeCaption}"
                        Margin="4,8,0,0"
                        Panel.ZIndex="10"
                        Visibility="Collapsed"/>

                <!-- RIGHT PANEL: sessions + tunnel bottom panel -->
                <Grid x:Name="Mw_SessionsGrid" Grid.Column="2"
                      ClipToBounds="True">
                    <Grid.RowDefinitions>
                        <!-- Row 0: Sessions / server details -->
                        <RowDefinition Height="*"/>
                        <!-- Row 1: GridSplitter (visible when tunnel panel open) -->
                        <RowDefinition Height="Auto"/>
                        <!-- Row 2: Retractable tunnel panel -->
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- SESSIONS (visible when active) -->
                    <TabControl x:Name="SessionTabControl"
                                Grid.Row="0"
                                ItemsSource="{Binding Connection.ActiveSessions}"
                                SelectedItem="{Binding Connection.ActiveSession}"
                                Visibility="{Binding Connection.HasActiveSessions, Converter={StaticResource BoolToVisibilityConverter}}"
                                Background="{DynamicResource BackgroundBrush}"
                                BorderThickness="0"
                                ClipToBounds="True"
                                Style="{DynamicResource ThemedTabControlStyle}"
                                PreviewMouseRightButtonDown="OnSessionTabRightClick"
                                PreviewMouseLeftButtonDown="OnTabDragStart"
                                PreviewMouseMove="OnTabDragMove"
                                DragOver="OnTabDragOver"
                                Drop="OnTabDrop"
                                AllowDrop="True"
                                ItemContainerStyle="{DynamicResource ThemedTabItemStyle}">
                        <TabControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:SessionTabViewModel}">
                                <Border x:Name="TabHeaderBorder" Padding="10,6" Background="Transparent"
                                        ToolTip="{Binding HeaderToolTip}">
                                    <DockPanel LastChildFill="True">
                                        <!-- Close button always occupies layout space (Hidden, not Collapsed)
                                             so the tab width stays constant on hover. Only opacity toggles.
                                             IsHitTestVisible stays True; the Border's Background="Transparent"
                                             ensures IsMouseOver persists when hovering over child elements. -->
                                        <Button x:Name="TabCloseButton" DockPanel.Dock="Right"
                                                Width="20" Height="20" Margin="6,0,0,0" Padding="2"
                                                Focusable="False" Content="&#xE711;"
                                                FontFamily="Segoe MDL2 Assets" FontSize="{StaticResource FontSizeSmallCaption}"
                                                Opacity="0.3" IsHitTestVisible="True"
                                                Command="{Binding DataContext.Connection.CloseSessionCommand, RelativeSource={RelativeSource AncestorType=TabControl}}"
                                                CommandParameter="{Binding}"
                                                AutomationProperties.Name="{Binding Tag, RelativeSource={RelativeSource AncestorType=TabControl}}"
                                                Style="{DynamicResource SecondaryButtonStyle}"/>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Path Data="{Binding ConnectionType, Converter={StaticResource TypeToGeoConverter}}"
                                                  Style="{StaticResource GeoIconSmall}"
                                                  VerticalAlignment="Center" Margin="0,0,5,0">
                                                <Path.Fill>
                                                    <MultiBinding Converter="{StaticResource TypeToColorConverter}">
                                                        <Binding Path="ConnectionType"/>
                                                        <Binding Path="DataContext.ThemeRevision" ElementName="MainWindowRoot"/>
                                                    </MultiBinding>
                                                </Path.Fill>
                                            </Path>
                                            <TextBlock Text="{Binding Title}" FontSize="{StaticResource FontSizeBody}" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                                       VerticalAlignment="Center"
                                                       ToolTip="{Binding HeaderToolTip}"
                                                       TextTrimming="CharacterEllipsis" MaxWidth="160"/>
                                            <TextBlock x:Name="BusyIndicator" Text="&#x25CF;" FontSize="8"
                                                       Foreground="{DynamicResource AccentBrush}"
                                                       VerticalAlignment="Center" Margin="5,0,0,0"
                                                       Visibility="Collapsed"
                                                       AutomationProperties.Name="{Binding Tag, RelativeSource={RelativeSource AncestorType=Grid, AncestorLevel=2}}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Resources>
                                                            <Storyboard x:Key="PulseAnimation">
                                                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                 From="1" To="0.2" Duration="0:0:0.6"
                                                                                 AutoReverse="True" RepeatBehavior="Forever"/>
                                                            </Storyboard>
                                                        </Style.Resources>
                                                        <Style.Triggers>
                                                            <Trigger Property="Visibility" Value="Visible">
                                                                <Trigger.EnterActions>
                                                                    <BeginStoryboard Name="PulseStoryboard" Storyboard="{StaticResource PulseAnimation}"/>
                                                                </Trigger.EnterActions>
                                                                <Trigger.ExitActions>
                                                                    <StopStoryboard BeginStoryboardName="PulseStoryboard"/>
                                                                </Trigger.ExitActions>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </DockPanel>
                                </Border>
                                <DataTemplate.Triggers>
                                    <!-- Show close button at full opacity on hover or active tab -->
                                    <Trigger SourceName="TabHeaderBorder" Property="IsMouseOver" Value="True">
                                        <Setter TargetName="TabCloseButton" Property="Opacity" Value="1"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TabItem}}" Value="True">
                                        <Setter TargetName="TabCloseButton" Property="Opacity" Value="1"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                        <Setter TargetName="BusyIndicator" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </TabControl.ItemTemplate>
                        <TabControl.ContentTemplate>
                            <DataTemplate DataType="{x:Type vm:SessionTabViewModel}">
                                <Grid>
                                    <ContentPresenter Content="{Binding RootContent}" ClipToBounds="True"/>
                                    <!-- Loading overlay (visible while HostControl is null during connection) -->
                                    <Border x:Name="SessionLoadingOverlay"
                                            Style="{StaticResource FadeInPanelStyle}"
                                            Background="{DynamicResource OverlayBackground}"
                                            Visibility="Collapsed">
                                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <ProgressBar IsIndeterminate="True"
                                                         Width="200" Height="3"
                                                         Margin="0,0,0,12"/>
                                            <TextBlock Text="{Binding Status}"
                                                       FontSize="{StaticResource FontSizeBodyLarge}"
                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="{Binding TunnelRoute}"
                                                       FontSize="{StaticResource FontSizeCaption}"
                                                       Foreground="{DynamicResource TextDisabledBrush}"
                                                       HorizontalAlignment="Center"
                                                       Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding HostControl}" Value="{x:Null}">
                                        <Setter TargetName="SessionLoadingOverlay" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </TabControl.ContentTemplate>
                    </TabControl>

                    <!-- Drop zone overlay for drag-to-split (shown during tab drag over content area) -->
                    <Border x:Name="ContentDropZone" Grid.Row="0"
                            Visibility="Collapsed"
                            Background="{DynamicResource DragDropOverlayBackground}"
                            BorderBrush="{DynamicResource AccentBrush}"
                            BorderThickness="2"
                            CornerRadius="4"
                            IsHitTestVisible="False"
                            Margin="0,32,0,0">
                        <TextBlock Text="{Binding DataContext.DropToMergeText, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue='Drop to split'}"
                                   FontSize="{StaticResource FontSizeSubtitle}"
                                   Foreground="{DynamicResource AccentBrush}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Opacity="0.8"/>
                    </Border>

                    <!-- SERVER DETAILS (visible when no sessions) -->
                    <Grid Grid.Row="0"
                          Visibility="{Binding Connection.HasActiveSessions, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                        <!-- Server detail panel (shown when a non-tool server is selected) -->
                        <Border x:Name="SessionDetailPanel"
                                Visibility="{Binding ServerList.HasSelection, Converter={StaticResource BoolToVisibilityConverter}}"
                                Padding="{StaticResource SpacingXl}">
                            <StackPanel VerticalAlignment="Top">
                                <TextBlock Text="{Binding ServerList.SelectedServer.DisplayName}"
                                           FontSize="{StaticResource FontSizeDisplay}" FontWeight="Bold"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           TextTrimming="CharacterEllipsis"/>

                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <Border CornerRadius="{StaticResource CornerRadiusSm}" Padding="6,2" Margin="0,0,8,0">
                                        <Border.Background>
                                            <MultiBinding Converter="{StaticResource ConnectionTypeToBrushConverter}">
                                                <Binding Path="ServerList.SelectedServer.ConnectionType"/>
                                                <Binding Path="DataContext.ThemeRevision" ElementName="MainWindowRoot"/>
                                            </MultiBinding>
                                        </Border.Background>
                                        <TextBlock Text="{Binding ServerList.SelectedServer.ConnectionType}"
                                                   FontSize="{StaticResource FontSizeCaption}" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource BadgeTextBrush}"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </Border>
                                    <Ellipse Width="8" Height="8" Margin="0,0,6,0"
                                             VerticalAlignment="Center">
                                        <Ellipse.Fill>
                                            <MultiBinding Converter="{StaticResource ConnectionStateToBrushConverter}">
                                                <Binding Path="ServerList.SelectedServer.ConnectionState"/>
                                                <Binding Path="DataContext.ThemeRevision" ElementName="MainWindowRoot"/>
                                            </MultiBinding>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    <TextBlock Text="{Binding ServerList.SelectedServer.ConnectionState}"
                                               FontSize="{StaticResource FontSizeCaption}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>

                                <TextBlock x:Name="Mw_DetailHostPort"
                                           Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,12,0,0"
                                           FontSize="{StaticResource FontSizeBodyLarge}">
                                    <Run Text="{Binding ServerList.SelectedServer.RemoteServer, Mode=OneWay}"/>
                                    <Run Text=" : "/>
                                    <Run Text="{Binding ServerList.SelectedServer.RemotePort, Mode=OneWay}"/>
                                </TextBlock>

                                <TextBlock Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"
                                           FontSize="{StaticResource FontSizeBody}"
                                           Visibility="{Binding ServerList.SelectedServer.Group, Converter={StaticResource NullToVisibilityConverter}}">
                                    <Run x:Name="Mw_DetailGroupLabel" Text=""/>
                                    <Run Text="{Binding ServerList.SelectedServer.Group, Mode=OneWay}"/>
                                </TextBlock>

                                <TextBlock Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"
                                           FontSize="{StaticResource FontSizeBody}"
                                           Visibility="{Binding ServerList.SelectedServer.Environment, Converter={StaticResource NullToVisibilityConverter}}">
                                    <Run x:Name="Mw_DetailEnvLabel" Text=""/>
                                    <Run Text="{Binding ServerList.SelectedServer.Environment, Mode=OneWay}"/>
                                </TextBlock>

                                <!-- Enriched metadata section -->
                                <Border Margin="0,12,0,0" Padding="12,8"
                                        Background="{DynamicResource CardBrush}"
                                        CornerRadius="{StaticResource CornerRadiusSm}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Project -->
                                        <TextBlock x:Name="Mw_DetailProjectLabel" Grid.Row="0" Grid.Column="0"
                                                   Text="" FontSize="{StaticResource FontSizeCaption}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   Margin="0,2,12,2" VerticalAlignment="Center"
                                                   Visibility="{Binding ServerList.SelectedServer.ProjectName, Converter={StaticResource NullToVisibilityConverter}}"/>
                                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal"
                                                    Visibility="{Binding ServerList.SelectedServer.ProjectName, Converter={StaticResource NullToVisibilityConverter}}"
                                                    Margin="0,2,0,2">
                                            <Ellipse Width="8" Height="8" Margin="0,0,6,0" VerticalAlignment="Center"
                                                     Fill="{Binding ServerList.SelectedServer.ProjectColor, Converter={StaticResource StringToBrushConverter}, TargetNullValue=#A67AF7}"/>
                                            <TextBlock Text="{Binding ServerList.SelectedServer.ProjectName}"
                                                       FontSize="{StaticResource FontSizeCaption}"
                                                       Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>

                                        <!-- Username -->
                                        <TextBlock x:Name="Mw_DetailUsernameLabel" Grid.Row="1" Grid.Column="0"
                                                   Text="" FontSize="{StaticResource FontSizeCaption}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   Margin="0,2,12,2" VerticalAlignment="Center"
                                                   Visibility="{Binding ServerList.SelectedServer.Username, Converter={StaticResource NullToVisibilityConverter}}"/>
                                        <TextBlock Grid.Row="1" Grid.Column="1"
                                                   Text="{Binding ServerList.SelectedServer.Username}"
                                                   FontSize="{StaticResource FontSizeCaption}"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   Margin="0,2,0,2"
                                                   Visibility="{Binding ServerList.SelectedServer.Username, Converter={StaticResource NullToVisibilityConverter}}"/>

                                        <!-- Gateway -->
                                        <TextBlock x:Name="Mw_DetailGatewayLabel" Grid.Row="2" Grid.Column="0"
                                                   Text="" FontSize="{StaticResource FontSizeCaption}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   Margin="0,2,12,2" VerticalAlignment="Center"
                                                   Visibility="{Binding ServerList.SelectedServer.GatewayName, Converter={StaticResource NullToVisibilityConverter}}"/>
                                        <TextBlock Grid.Row="2" Grid.Column="1"
                                                   FontSize="{StaticResource FontSizeCaption}"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   Margin="0,2,0,2"
                                                   Visibility="{Binding ServerList.SelectedServer.GatewayName, Converter={StaticResource NullToVisibilityConverter}}">
                                            <Run Text="{Binding ServerList.SelectedServer.GatewayName, Mode=OneWay}"/>
                                        </TextBlock>

                                        <!-- Authentication -->
                                        <TextBlock x:Name="Mw_DetailAuthLabel" Grid.Row="3" Grid.Column="0"
                                                   Text="" FontSize="{StaticResource FontSizeCaption}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   Margin="0,2,12,2" VerticalAlignment="Center"
                                                   Visibility="{Binding ServerList.SelectedServer.AuthSummary, Converter={StaticResource NullToVisibilityConverter}}"/>
                                        <TextBlock Grid.Row="3" Grid.Column="1"
                                                   Text="{Binding ServerList.SelectedServer.AuthSummary}"
                                                   FontSize="{StaticResource FontSizeCaption}"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   Margin="0,2,0,2"
                                                   Visibility="{Binding ServerList.SelectedServer.AuthSummary, Converter={StaticResource NullToVisibilityConverter}}"/>

                                        <!-- Tags -->
                                        <TextBlock x:Name="Mw_DetailTagsLabel" Grid.Row="4" Grid.Column="0"
                                                   Text="" FontSize="{StaticResource FontSizeCaption}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   Margin="0,2,12,2" VerticalAlignment="Center"
                                                   Visibility="{Binding ServerList.SelectedServer.Tags, Converter={StaticResource NullToVisibilityConverter}}"/>
                                        <TextBlock Grid.Row="4" Grid.Column="1"
                                                   Text="{Binding ServerList.SelectedServer.Tags}"
                                                   FontSize="{StaticResource FontSizeCaption}"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   Margin="0,2,0,2" TextWrapping="Wrap"
                                                   Visibility="{Binding ServerList.SelectedServer.Tags, Converter={StaticResource NullToVisibilityConverter}}"/>

                                        <!-- Favorite -->
                                        <TextBlock x:Name="Mw_DetailFavoriteLabel" Grid.Row="5" Grid.Column="0"
                                                   Text="" FontSize="{StaticResource FontSizeCaption}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   Margin="0,2,12,2" VerticalAlignment="Center"
                                                   Visibility="{Binding ServerList.SelectedServer.IsFavorite, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        <TextBlock Grid.Row="5" Grid.Column="1"
                                                   Text="&#xE735;" FontFamily="Segoe MDL2 Assets"
                                                   FontSize="{StaticResource FontSizeBody}"
                                                   Foreground="{DynamicResource WarningBrush}"
                                                   Margin="0,2,0,2"
                                                   Visibility="{Binding ServerList.SelectedServer.IsFavorite, Converter={StaticResource BoolToVisibilityConverter}}"
                                                   AutomationProperties.Name="Favorite"/>
                                    </Grid>
                                </Border>

                                <Button x:Name="Mw_DetailConnectBtn" Content=""
                                        Command="{Binding ServerList.ConnectCommand}"
                                        CommandParameter="{Binding ServerList.SelectedServer}"
                                        Style="{DynamicResource PrimaryButtonStyle}"
                                        Width="120" HorizontalAlignment="Left"
                                        Margin="0,20,0,0"/>
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <Button x:Name="Mw_DetailEditBtn" Content=""
                                            Command="{Binding ServerList.EditServerCommand}"
                                            CommandParameter="{Binding ServerList.SelectedServer}"
                                            Style="{DynamicResource SecondaryButtonStyle}"
                                            Padding="12,6" Margin="0,0,8,0"/>
                                    <Button x:Name="Mw_DetailDeleteBtn" Content=""
                                            Command="{Binding ServerList.DeleteServerCommand}"
                                            CommandParameter="{Binding ServerList.SelectedServer}"
                                            Style="{DynamicResource SecondaryButtonStyle}"
                                            Foreground="{DynamicResource ErrorTextBrush}"
                                            Padding="12,6"/>
                                </StackPanel>
                                <!-- Subtle shortcut hints for detail panel actions -->
                                <TextBlock x:Name="Mw_DetailActionHints" Text=""
                                           FontSize="{StaticResource FontSizeSmallCaption}"
                                           Foreground="{DynamicResource TextDisabledBrush}"
                                           Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Tool detail panel (shown when a TOOL:* entry is selected) -->
                        <Border x:Name="ToolDetailPanel"
                                Visibility="Collapsed"
                                Padding="{StaticResource SpacingXl}">
                            <StackPanel VerticalAlignment="Top">
                                <TextBlock x:Name="Mw_ToolDetailName"
                                           FontSize="{StaticResource FontSizeDisplay}" FontWeight="Bold"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           TextTrimming="CharacterEllipsis"/>

                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <Border CornerRadius="{StaticResource CornerRadiusSm}" Padding="6,2" Margin="0,0,8,0"
                                            Background="{DynamicResource ToolBadgeBrush}">
                                        <TextBlock x:Name="Mw_ToolDetailCategory"
                                                   FontSize="{StaticResource FontSizeCaption}" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource BadgeTextBrush}"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </Border>
                                </StackPanel>

                                <TextBlock x:Name="Mw_ToolDetailDescription"
                                           Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,12,0,0"
                                           FontSize="{StaticResource FontSizeBodyLarge}" TextWrapping="Wrap"/>

                                <Button x:Name="Mw_ToolDetailOpenBtn" Content=""
                                        Command="{Binding ServerList.ConnectCommand}"
                                        CommandParameter="{Binding ServerList.SelectedServer}"
                                        Style="{DynamicResource PrimaryButtonStyle}"
                                        Width="140" HorizontalAlignment="Left"
                                        Margin="0,20,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Empty state when nothing selected -->
                        <StackPanel Visibility="{Binding ServerList.HasSelection, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"
                                    VerticalAlignment="Center" HorizontalAlignment="Center"
                                    IsHitTestVisible="True">
                            <!-- Show empty state overlay when no servers exist -->
                            <StackPanel Visibility="{Binding ServerList.IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="&#xE774;"
                                           FontFamily="Segoe MDL2 Assets" FontSize="{StaticResource FontSizeHero}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           HorizontalAlignment="Center" Opacity="0.5"
                                           Margin="0,0,0,16"
                                           AutomationProperties.Name=""/>
                                <TextBlock x:Name="Mw_EmptyStateTitle" Text="" FontSize="{StaticResource FontSizeHeadline}"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           HorizontalAlignment="Center"/>
                                <TextBlock x:Name="Mw_EmptyStateSubtitle" Text=""
                                           FontSize="{StaticResource FontSizeBodyLarge}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           HorizontalAlignment="Center" Margin="0,8,0,24"
                                           TextWrapping="Wrap" MaxWidth="400"
                                           TextAlignment="Center"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button x:Name="Mw_EmptyBtnAddSession" Content=""
                                            Command="{Binding ServerList.AddServerCommand}"
                                            Style="{DynamicResource PrimaryButtonStyle}"
                                            Padding="16,8" FontSize="{StaticResource FontSizeBodyLarge}"/>
                                    <Button x:Name="Mw_EmptyBtnImport" Content=""
                                            Command="{Binding Settings.ImportConfigCommand}"
                                            Style="{DynamicResource SecondaryButtonStyle}"
                                            Padding="16,8" FontSize="{StaticResource FontSizeBodyLarge}"
                                            Margin="12,0,0,0"/>
                                    <Button x:Name="Mw_EmptyBtnExploreTools" Content=""
                                            Style="{DynamicResource SecondaryButtonStyle}"
                                            Padding="16,8" FontSize="{StaticResource FontSizeBodyLarge}"
                                            Margin="12,0,0,0"
                                            Click="OnEmptyExploreToolsClick"/>
                                </StackPanel>
                                <!-- Keyboard shortcut hints for discoverability -->
                                <TextBlock x:Name="Mw_EmptyStateShortcutHints" Text=""
                                           FontSize="{StaticResource FontSizeSmallCaption}"
                                           Foreground="{DynamicResource TextDisabledBrush}"
                                           HorizontalAlignment="Center" Margin="0,16,0,0"
                                           TextAlignment="Center"/>
                            </StackPanel>

                            <!-- Show hint when servers exist but none selected -->
                            <Border Visibility="{Binding ServerList.IsEmpty, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"
                                    Style="{DynamicResource EmptyStateStyle}">
                                <StackPanel HorizontalAlignment="Center"
                                            VerticalAlignment="Center">
                                    <TextBlock Text="&#xE8B7;"
                                               FontFamily="Segoe MDL2 Assets"
                                               FontSize="48"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               HorizontalAlignment="Center"
                                               Opacity="0.5"
                                               Margin="0,0,0,12"
                                               AutomationProperties.Name=""/>
                                    <TextBlock x:Name="Mw_EmptySelectSession" Text=""
                                               HorizontalAlignment="Center"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="{StaticResource FontSizeSubtitle}"
                                               TextAlignment="Center"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,4"/>
                                    <TextBlock x:Name="Mw_EmptyQuickConnectHint" Text=""
                                               HorizontalAlignment="Center"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="{StaticResource FontSizeCaption}"
                                               TextAlignment="Center"
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <!-- TUNNEL BOTTOM PANEL: GridSplitter + compact active tunnels -->
                    <GridSplitter Grid.Row="1"
                                  Height="4"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Center"
                                  Background="{DynamicResource CardBrush}"
                                  ShowsPreview="True"
                                  Visibility="{Binding IsTunnelPanelOpen, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <Border Grid.Row="2"
                            MinHeight="120" MaxHeight="300"
                            Background="{DynamicResource SurfaceBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="0,1,0,0"
                            Visibility="{Binding IsTunnelPanelOpen, Converter={StaticResource BoolToVisibilityConverter}}">
                        <DockPanel>
                            <!-- Header: "Tunnels (N)" + collapse button -->
                            <Border DockPanel.Dock="Top"
                                    Padding="8,4"
                                    Background="{DynamicResource SurfaceBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="0,0,0,1">
                                <DockPanel>
                                    <Button x:Name="Mw_TunnelPanelCollapseBtn" DockPanel.Dock="Right"
                                            Content="&#xE70D;"
                                            FontFamily="Segoe MDL2 Assets"
                                            FontSize="{StaticResource FontSizeCaption}"
                                            Command="{Binding ToggleTunnelPanelCommand}"
                                            Style="{DynamicResource ToolbarGhostButtonStyle}"
                                            Padding="6,2" Margin="4,0,0,0"
                                            AutomationProperties.Name="{loc:Translate A11yCollapseTunnelPanel}"/>
                                    <Button x:Name="Mw_TunnelPanelCloseAllBtn" DockPanel.Dock="Right"
                                            Content=""
                                            Command="{Binding CloseAllTunnelsCommand}"
                                            Style="{DynamicResource SecondaryButtonStyle}"
                                            Foreground="{DynamicResource ErrorTextBrush}"
                                            Padding="6,2" FontSize="{StaticResource FontSizeCaption}" Margin="4,0,0,0"
                                            AutomationProperties.Name="{loc:Translate A11yCloseAllTunnels}"/>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="&#xE945;" FontFamily="Segoe MDL2 Assets"
                                                   FontSize="{StaticResource FontSizeBody}" Margin="0,0,6,0"
                                                   Foreground="{DynamicResource AccentBrush}"
                                                   VerticalAlignment="Center"
                                                   AutomationProperties.Name=""/>
                                        <TextBlock FontSize="{StaticResource FontSizeBody}" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   VerticalAlignment="Center">
                                            <Run x:Name="Mw_TunnelPanelHeaderPrefix" Text=""/>
                                            <Run Text="{Binding TunnelCount, Mode=OneWay}"/>
                                            <Run x:Name="Mw_TunnelPanelHeaderSuffix" Text=""/>
                                        </TextBlock>
                                    </StackPanel>
                                </DockPanel>
                            </Border>

                            <!-- Content: compact tunnel DataGrid -->
                            <Grid>
                                <DataGrid x:Name="Mw_TunnelPanelGrid"
                                          ItemsSource="{Binding TunnelList}"
                                          SelectedItem="{Binding SelectedTunnel}"
                                          AutoGenerateColumns="False"
                                          IsReadOnly="True"
                                          SelectionMode="Single"
                                          Margin="0"
                                          Style="{DynamicResource ThemedDataGridStyle}"
                                          ColumnHeaderStyle="{DynamicResource ThemedDataGridColumnHeaderStyle}"
                                          CellStyle="{DynamicResource ThemedDataGridCellStyle}"
                                          RowStyle="{DynamicResource ThemedDataGridRowStyle}"
                                          HeadersVisibility="Column"
                                          GridLinesVisibility="None"
                                          RowHeight="24"
                                          FontSize="{StaticResource FontSizeCaption}">
                                    <DataGrid.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem x:Name="Mw_TpCtxClose"
                                                      Command="{Binding CloseTunnelCommand}"
                                                      CommandParameter="{Binding SelectedTunnel}"/>
                                            <MenuItem x:Name="Mw_TpCtxCopyPort"
                                                      Command="{Binding CopyTunnelPortCommand}"
                                                      CommandParameter="{Binding SelectedTunnel}"/>
                                            <Separator/>
                                            <MenuItem x:Name="Mw_TpCtxCloseAll"
                                                      Command="{Binding CloseAllTunnelsCommand}"/>
                                        </ContextMenu>
                                    </DataGrid.ContextMenu>
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Header="" Width="24">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Ellipse Width="7" Height="7"
                                                             HorizontalAlignment="Center"
                                                             VerticalAlignment="Center">
                                                        <Ellipse.Style>
                                                            <Style TargetType="Ellipse">
                                                                <Setter Property="Fill" Value="{DynamicResource SuccessBrush}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsAlive}" Value="False">
                                                                        <Setter Property="Fill" Value="{DynamicResource ErrorBrush}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Ellipse.Style>
                                                    </Ellipse>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn x:Name="Mw_TpColGateway" Header="" Binding="{Binding ServerName}" Width="*"/>
                                        <DataGridTextColumn x:Name="Mw_TpColLocal" Header="" Binding="{Binding LocalPort}" Width="60"/>
                                        <DataGridTextColumn x:Name="Mw_TpColRemote" Header="" Binding="{Binding RemoteHost}" Width="*"/>
                                        <DataGridTextColumn x:Name="Mw_TpColPort" Header="" Binding="{Binding RemotePort}" Width="50"/>
                                        <DataGridTemplateColumn Header="" Width="28">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="&#xE711;"
                                                            FontFamily="Segoe MDL2 Assets"
                                                            FontSize="{StaticResource FontSizeCaption}"
                                                            Command="{Binding DataContext.CloseTunnelCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                            CommandParameter="{Binding}"
                                                            Style="{DynamicResource ToolbarGhostButtonStyle}"
                                                            Padding="2" Margin="0"
                                                            ToolTip="{Binding Tag, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                            VerticalAlignment="Center"
                                                            HorizontalAlignment="Center"
                                                            AutomationProperties.Name="{Binding Tag, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <!-- Empty state -->
                                <TextBlock x:Name="Mw_TunnelPanelEmpty" Text=""
                                           FontSize="{StaticResource FontSizeCaption}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Visibility="{Binding HasNoTunnels, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </Grid>
                        </DockPanel>
                    </Border>
                </Grid>
            </Grid>

            <!-- ============================================================ -->
            <!-- TUNNELS TAB: Active tunnel list                              -->
            <!-- ============================================================ -->
            <DockPanel Visibility="{Binding IsTunnelsTabSelected, Converter={StaticResource BoolToVisibilityConverter}}">
                <DockPanel DockPanel.Dock="Top" Margin="16,12,16,8">
                    <TextBlock x:Name="Mw_TunnelsTitle" Text="" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="Mw_TunnelsCloseSelectedBtn" Content=""
                                Command="{Binding CloseTunnelCommand}"
                                CommandParameter="{Binding SelectedTunnel}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Padding="8,3" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,4,0"/>
                        <Button x:Name="Mw_TunnelsCloseAllBtn" Content=""
                                Command="{Binding CloseAllTunnelsCommand}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Foreground="{DynamicResource ErrorTextBrush}"
                                Padding="8,3" FontSize="{StaticResource FontSizeCaption}"/>
                    </StackPanel>
                </DockPanel>

                <!-- Manage gateways link -->
                <Button x:Name="Mw_TunnelsManageGatewaysLink" DockPanel.Dock="Bottom"
                        Content=""
                        Style="{DynamicResource ToolbarGhostButtonStyle}"
                        FontSize="{StaticResource FontSizeCaption}"
                        Foreground="{DynamicResource AccentBrush}"
                        HorizontalAlignment="Center"
                        Margin="0,8,0,8"
                        Click="OnNavigateToGatewaySettings"/>

                <Grid>
                    <!-- Tunnel DataGrid -->
                    <DataGrid ItemsSource="{Binding TunnelList}"
                              SelectedItem="{Binding SelectedTunnel}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              Margin="16,0,16,8"
                              Style="{DynamicResource ThemedDataGridStyle}"
                              ColumnHeaderStyle="{DynamicResource ThemedDataGridColumnHeaderStyle}"
                              CellStyle="{DynamicResource ThemedDataGridCellStyle}"
                              RowStyle="{DynamicResource ThemedDataGridRowStyle}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="" Width="28">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Ellipse Width="8" Height="8"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center">
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Setter Property="Fill" Value="{DynamicResource SuccessBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsAlive}" Value="False">
                                                            <Setter Property="Fill" Value="{DynamicResource ErrorBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn x:Name="Mw_TunnelsColGateway" Header="" Binding="{Binding ServerName}" Width="*"/>
                            <DataGridTextColumn x:Name="Mw_TunnelsColLocalPort" Header="" Binding="{Binding LocalPort}" Width="90"/>
                            <DataGridTextColumn x:Name="Mw_TunnelsColRemoteHost" Header="" Binding="{Binding RemoteHost}" Width="*"/>
                            <DataGridTextColumn x:Name="Mw_TunnelsColRemotePort" Header="" Binding="{Binding RemotePort}" Width="90"/>
                            <DataGridTextColumn x:Name="Mw_TunnelsColStarted" Header="" Binding="{Binding StartedAt, StringFormat=HH:mm:ss}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Empty state -->
                    <TextBlock x:Name="Mw_TunnelsEmpty" Text=""
                               FontSize="{StaticResource FontSizeBodyLarge}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="{Binding HasNoTunnels, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>
            </DockPanel>

            <!-- ============================================================ -->
            <!-- SCHEDULED TAB: Scheduled connection management               -->
            <!-- ============================================================ -->
            <DockPanel Visibility="{Binding IsScheduledTabSelected, Converter={StaticResource BoolToVisibilityConverter}}">
                <!-- Header bar -->
                <DockPanel DockPanel.Dock="Top" Margin="16,12,16,8">
                    <TextBlock x:Name="Mw_ScheduledTitle" Text="" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="Mw_ScheduledAddBtn" Content=""
                                Command="{Binding AddScheduledTaskCommand}"
                                Style="{DynamicResource PrimaryButtonStyle}"
                                Padding="8,3" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,4,0"/>
                        <Button x:Name="Mw_ScheduledEditBtn" Content=""
                                Command="{Binding EditScheduledTaskCommand}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Padding="8,3" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,4,0"/>
                        <Button x:Name="Mw_ScheduledDeleteBtn" Content=""
                                Command="{Binding DeleteScheduledTaskCommand}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                Padding="8,3" FontSize="{StaticResource FontSizeCaption}"/>
                    </StackPanel>
                </DockPanel>

                <Grid>
                    <!-- Scheduled tasks DataGrid -->
                    <DataGrid ItemsSource="{Binding ScheduledTasks}"
                              SelectedItem="{Binding SelectedScheduledTask}"
                              AutoGenerateColumns="False"
                              SelectionMode="Single"
                              Margin="16,0,16,8"
                              MouseDoubleClick="OnScheduledTaskDoubleClick"
                              Style="{DynamicResource ThemedDataGridStyle}"
                              ColumnHeaderStyle="{DynamicResource ThemedDataGridColumnHeaderStyle}"
                              CellStyle="{DynamicResource ThemedDataGridCellStyle}"
                              RowStyle="{DynamicResource ThemedDataGridRowStyle}">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn x:Name="Mw_ScheduledColEnabled" Header="" Binding="{Binding Enabled, UpdateSourceTrigger=PropertyChanged}" Width="40"/>
                            <DataGridTextColumn x:Name="Mw_ScheduledColServer" Header="" Binding="{Binding ServerName}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn x:Name="Mw_ScheduledColType" Header="" Binding="{Binding ConnectionType}" Width="70" IsReadOnly="True"/>
                            <DataGridTextColumn x:Name="Mw_ScheduledColSchedule" Header="" Binding="{Binding Schedule}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn x:Name="Mw_ScheduledColLastRun" Header="" Binding="{Binding LastRun, StringFormat=yyyy-MM-dd HH:mm}" Width="130" IsReadOnly="True"/>
                            <DataGridTextColumn x:Name="Mw_ScheduledColNextRun" Header="" Binding="{Binding NextRun, StringFormat=yyyy-MM-dd HH:mm}" Width="130" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Empty state -->
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                                Visibility="{Binding HasNoScheduledTasks, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock x:Name="Mw_ScheduledEmpty" Text=""
                                   FontSize="{StaticResource FontSizeBodyLarge}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <Button x:Name="Mw_ScheduledCreateBtn" Content=""
                                Command="{Binding AddScheduledTaskCommand}"
                                Style="{DynamicResource PrimaryButtonStyle}"
                                Padding="12,6" FontSize="{StaticResource FontSizeBody}"
                                HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </DockPanel>

            <!-- ============================================================ -->
            <!-- TOOLS TAB: Dedicated tool browser                             -->
            <!-- ============================================================ -->
            <Grid Visibility="{Binding IsToolsTabSelected, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header: Title + search + tool count -->
                <Border Grid.Row="0" Padding="16,12,16,8"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,0,0,1">
                    <StackPanel>
                        <DockPanel>
                            <TextBlock x:Name="Mw_ToolsTabTitle"
                                       Text="{Binding ToolsTab.HeaderTitle}"
                                       FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       VerticalAlignment="Center" DockPanel.Dock="Left"
                                       Margin="0,0,16,0"/>
                            <TextBlock x:Name="Mw_ToolsTabCount"
                                       Text="{Binding ToolsTab.CountLabel}"
                                       FontSize="{StaticResource FontSizeCaption}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       VerticalAlignment="Center" DockPanel.Dock="Right"
                                       HorizontalAlignment="Right"/>
                            <TextBox x:Name="Mw_ToolsTabSearch"
                                     Style="{StaticResource WatermarkTextBoxStyle}"
                                     Tag="{Binding ToolsTab.SearchPlaceholder}"
                                     Text="{Binding ToolsTab.SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     FontSize="{StaticResource FontSizeBody}"
                                     Padding="{StaticResource PaddingInput}"
                                     Background="{DynamicResource CardBrush}"
                                     Foreground="{DynamicResource TextPrimaryBrush}"
                                     BorderBrush="{DynamicResource BorderBrush}"
                                     MaxWidth="400" HorizontalAlignment="Left"
                                     AutomationProperties.Name="{loc:Translate ToolsTabSearchPlaceholder}"/>
                        </DockPanel>
                        <TextBlock x:Name="Mw_ToolsTabContextText"
                                   Text="{Binding ToolsTab.ContextLabel}"
                                   ToolTip="{Binding ToolsTab.ContextTooltip}"
                                   Foreground="{Binding ToolsTab.ContextBrushKey, Converter={StaticResource ResourceKeyToBrushConverter}}"
                                   Margin="0,8,0,0"
                                   FontSize="{StaticResource FontSizeCaption}"
                                   TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </Border>

                <!-- Content: scrollable tool grid with sections -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                              Padding="16,8,16,16">
                    <StackPanel x:Name="ToolsTabContent"/>
                </ScrollViewer>
            </Grid>

            <!-- ============================================================ -->
            <!-- SETTINGS TAB: Left-navigation sub-tabs                        -->
            <!-- ============================================================ -->
            <Grid Visibility="{Binding IsSettingsTabSelected, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Sticky action bar -->
                <Border Grid.Row="0"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="{StaticResource SpacingMd}">
                    <DockPanel>
                        <!-- Settings search box (right-aligned) -->
                        <Grid DockPanel.Dock="Right" Width="220" Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBox x:Name="Mw_SettingsSearchBox"
                                     Style="{DynamicResource SearchTextBoxStyle}"
                                     Tag="" Padding="10,5,28,5"
                                     TextChanged="OnSettingsSearchTextChanged"
                                     AutomationProperties.Name="{loc:Translate A11ySettingsSearch}"/>
                            <Button x:Name="Mw_SettingsSearchClear"
                                    Content="&#xE711;" FontFamily="Segoe MDL2 Assets"
                                    FontSize="{StaticResource FontSizeSmallCaption}"
                                    HorizontalAlignment="Right" VerticalAlignment="Center"
                                    Margin="0,0,4,0" Padding="4"
                                    Style="{DynamicResource ToolbarGhostButtonStyle}" MinHeight="0"
                                    Click="OnSettingsSearchClearClick"
                                    Visibility="Collapsed"
                                    AutomationProperties.Name="{loc:Translate A11yClearSearchFilter}"/>
                        </Grid>
                        <WrapPanel Orientation="Horizontal">
                            <Button x:Name="Mw_SettingsSaveBtn" Content="" Command="{Binding Settings.SaveCommand}"
                                    Style="{DynamicResource PrimaryButtonStyle}" MinWidth="130" Margin="0,0,16,4"/>
                            <Button x:Name="Mw_SettingsResetBtn" Content="" Command="{Binding Settings.ResetToDefaultsCommand}"
                                    Style="{DynamicResource SecondaryButtonStyle}" MinWidth="130" Margin="0,0,8,4"/>
                            <Button x:Name="Mw_SettingsExportBtn" Content="" Command="{Binding Settings.ExportConfigCommand}"
                                    Style="{DynamicResource SecondaryButtonStyle}" MinWidth="130" Margin="0,0,8,4"/>
                            <Button x:Name="Mw_SettingsImportBtn" Content="" Command="{Binding Settings.ImportConfigCommand}"
                                    Style="{DynamicResource SecondaryButtonStyle}" MinWidth="130" Margin="0,0,8,4"/>
                            <Button x:Name="Mw_SettingsCitrixBtn" Content="" Command="{Binding Settings.ImportCitrixAppsCommand}"
                                    Style="{DynamicResource SecondaryButtonStyle}" MinWidth="160" Margin="0,0,8,4"/>
                        </WrapPanel>
                    </DockPanel>
                </Border>

                <!-- Settings search hint (shown when filtering) -->
                <Border x:Name="Mw_SettingsSearchHint" Grid.Row="1" Visibility="Collapsed"
                        Padding="16,4" Background="{DynamicResource SurfaceBrush}">
                    <TextBlock x:Name="Mw_SettingsSearchHintText"
                               FontSize="{StaticResource FontSizeCaption}"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                </Border>

                <TabControl Grid.Row="2" x:Name="Mw_SettingsSubTabControl" TabStripPlacement="Top" Margin="16,8,16,0"
                            Background="Transparent" BorderThickness="0">

                    <!-- ═══════ GENERAL ═══════ -->
                    <TabItem x:Name="Mw_SettingsTabGeneral" Style="{StaticResource ThemedTabItemStyle}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="{StaticResource SpacingLg}" MaxWidth="900">
                                <Border Padding="{StaticResource SpacingLg}" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource CornerRadiusLg}" Margin="0,0,0,16">
                                    <StackPanel>
                                        <TextBlock x:Name="Mw_SettingsAppearanceTitle" Text="" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                                <TextBlock x:Name="Mw_SettingsLanguageLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                                <ComboBox SelectedValue="{Binding Settings.DefaultLocale}" SelectedValuePath="Content">
                                                    <ComboBoxItem Content="en"/>
                                                    <ComboBoxItem Content="fr"/>
                                                </ComboBox>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                                <TextBlock x:Name="Mw_SettingsThemeLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                                <ComboBox SelectedValue="{Binding Settings.DefaultTheme}" SelectedValuePath="Tag">
                                                    <ComboBoxItem x:Name="Mw_ThemeDraculaPro" Content="" Tag="DraculaPro"/>
                                                    <ComboBoxItem x:Name="Mw_ThemeAlucard" Content="" Tag="Alucard"/>
                                                    <ComboBoxItem x:Name="Mw_ThemeBlade" Content="" Tag="Blade"/>
                                                    <ComboBoxItem x:Name="Mw_ThemeBuffy" Content="" Tag="Buffy"/>
                                                    <ComboBoxItem x:Name="Mw_ThemeLincoln" Content="" Tag="Lincoln"/>
                                                    <ComboBoxItem x:Name="Mw_ThemeMorbius" Content="" Tag="Morbius"/>
                                                    <ComboBoxItem x:Name="Mw_ThemeVanHelsing" Content="" Tag="VanHelsing"/>
                                                </ComboBox>
                                            </StackPanel>
                                        </Grid>
                                        <StackPanel Margin="0,12,0,0">
                                            <TextBlock x:Name="Mw_SettingsMaxSessionsLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                            <TextBox Text="{Binding Settings.MaxEmbeddedSessions}" Width="100" HorizontalAlignment="Left"/>
                                        </StackPanel>
                                        <CheckBox x:Name="Mw_SettingsPreventSleep" Content="" IsChecked="{Binding Settings.PreventSleepDuringSession}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,12,0,0"/>
                                        <CheckBox x:Name="Mw_SettingsEnableSessionPersistence" Content="" IsChecked="{Binding Settings.EnableSessionPersistence}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,6,0,0"/>
                                        <StackPanel Margin="0,12,0,0">
                                            <TextBlock x:Name="Mw_SettingsEditorPathLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                            <DockPanel>
                                                <Button x:Name="Mw_SettingsBrowseEditorPath" DockPanel.Dock="Right"
                                                        Padding="6,2" Margin="4,0,0,0" Click="OnBrowseEditorPathClick"
                                                        Style="{DynamicResource SecondaryButtonStyle}"
                                                        FontSize="{StaticResource FontSizeCaption}"
                                                        AutomationProperties.Name="{loc:Translate A11yBrowseEditorPath}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE838;" FontSize="12" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{loc:Translate BtnBrowse}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Button>
                                                <TextBox Text="{Binding Settings.ExternalEditorPath}"/>
                                            </DockPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- ═══════ TERMINAL ═══════ -->
                    <TabItem x:Name="Mw_SettingsTabTerminal" Style="{StaticResource ThemedTabItemStyle}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="{StaticResource SpacingLg}" MaxWidth="900">
                                <Border Padding="{StaticResource SpacingLg}" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource CornerRadiusLg}">
                                    <StackPanel>
                                        <TextBlock x:Name="Mw_SettingsTerminalTitle" Text="" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                                <TextBlock x:Name="Mw_SettingsTerminalFontLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                                <TextBox Text="{Binding Settings.TerminalFontFamily}" Width="200" HorizontalAlignment="Left"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                                <TextBlock x:Name="Mw_SettingsTerminalFontSizeLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBox Text="{Binding Settings.TerminalFontSize}" Width="80"/>
                                                    <TextBlock Text="px" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" IsHitTestVisible="False"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                        <StackPanel Margin="0,12,0,0">
                                            <TextBlock x:Name="Mw_SettingsTerminalColorSchemeLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                            <ComboBox x:Name="Mw_ColorSchemeCombo" SelectedValue="{Binding Settings.TerminalColorScheme}" SelectedValuePath="Tag" Width="200" HorizontalAlignment="Left">
                                                <ComboBoxItem x:Name="Mw_SchemeDefault" Content="" Tag="Default"/>
                                                <ComboBoxItem x:Name="Mw_SchemeDracula" Content="" Tag="Dracula"/>
                                                <ComboBoxItem x:Name="Mw_SchemeSolarized" Content="" Tag="Solarized Dark"/>
                                                <ComboBoxItem x:Name="Mw_SchemeMonokai" Content="" Tag="Monokai"/>
                                                <ComboBoxItem x:Name="Mw_SchemeNord" Content="" Tag="Nord"/>
                                            </ComboBox>
                                        </StackPanel>
                                        <StackPanel Margin="0,12,0,0">
                                            <TextBlock x:Name="Mw_SettingsPsPolicyLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                            <ComboBox SelectedValue="{Binding Settings.PowerShellExecutionPolicy}" SelectedValuePath="Tag" Width="200" HorizontalAlignment="Left">
                                                <ComboBoxItem x:Name="Mw_PsDefault" Content="" Tag="Default"/>
                                                <ComboBoxItem x:Name="Mw_PsBypass" Content="" Tag="Bypass"/>
                                                <ComboBoxItem x:Name="Mw_PsRemoteSigned" Content="" Tag="RemoteSigned"/>
                                                <ComboBoxItem x:Name="Mw_PsUnrestricted" Content="" Tag="Unrestricted"/>
                                                <ComboBoxItem x:Name="Mw_PsAllSigned" Content="" Tag="AllSigned"/>
                                            </ComboBox>
                                            <TextBlock x:Name="Mw_SettingsPsPolicyHint" Text="" Foreground="{DynamicResource TextTertiaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- ═══════ SSH & SFTP ═══════ -->
                    <TabItem x:Name="Mw_SettingsTabSsh" Style="{StaticResource ThemedTabItemStyle}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="{StaticResource SpacingLg}" MaxWidth="900">
                                <Border Padding="{StaticResource SpacingLg}" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource CornerRadiusLg}" Margin="0,0,0,16">
                                    <StackPanel>
                                        <TextBlock x:Name="Mw_SettingsSshTitle" Text="" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                                        <TextBlock x:Name="Mw_SettingsPlinkPathLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                        <DockPanel>
                                            <Button x:Name="Mw_SettingsBrowsePlinkPath" DockPanel.Dock="Right"
                                                    Padding="6,2" Margin="4,0,0,0" Click="OnBrowsePlinkPathClick"
                                                    Style="{DynamicResource SecondaryButtonStyle}"
                                                    FontSize="{StaticResource FontSizeCaption}"
                                                    AutomationProperties.Name="{loc:Translate A11yBrowsePlinkPath}"
                                                    TabIndex="2">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE838;" FontSize="12" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{loc:Translate BtnBrowse}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Button>
                                            <TextBox Text="{Binding Settings.PlinkPath}" TabIndex="1" AutomationProperties.LabeledBy="{x:Reference Mw_SettingsPlinkPathLabel}"/>
                                        </DockPanel>
                                        <TextBlock x:Name="Mw_SettingsPlinkPathHint" Text="" Foreground="{DynamicResource TextTertiaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                        <TextBlock x:Name="Mw_SettingsPuttyPathLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,12,0,4"/>
                                        <DockPanel>
                                            <Button x:Name="Mw_SettingsBrowsePuttyPath" DockPanel.Dock="Right"
                                                    Padding="6,2" Margin="4,0,0,0" Click="OnBrowsePuttyPathClick"
                                                    Style="{DynamicResource SecondaryButtonStyle}"
                                                    FontSize="{StaticResource FontSizeCaption}"
                                                    AutomationProperties.Name="{loc:Translate A11yBrowsePuttyPath}"
                                                    TabIndex="4">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE838;" FontSize="12" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{loc:Translate BtnBrowse}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Button>
                                            <TextBox Text="{Binding Settings.PuttyPath}" TabIndex="3" AutomationProperties.LabeledBy="{x:Reference Mw_SettingsPuttyPathLabel}"/>
                                        </DockPanel>
                                        <TextBlock x:Name="Mw_SettingsPuttyPathHint" Text="" Foreground="{DynamicResource TextTertiaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                        <StackPanel Margin="0,12,0,0">
                                            <TextBlock x:Name="Mw_SettingsSshModeLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                            <StackPanel Orientation="Horizontal">
                                                <ComboBox SelectedValue="{Binding Settings.SshDefaultMode}" SelectedValuePath="Tag" Width="200" HorizontalAlignment="Left" AutomationProperties.LabeledBy="{x:Reference Mw_SettingsSshModeLabel}">
                                                    <ComboBoxItem x:Name="Mw_SshModeEmbedded" Content="" Tag="Embedded"/>
                                                    <ComboBoxItem x:Name="Mw_SshModeExternal" Content="" Tag="External"/>
                                                </ComboBox>
                                                <Button x:Name="Mw_ApplySshModeAll"
                                                        Content=""
                                                        Command="{Binding Settings.ApplySshModeToAllCommand}"
                                                        Style="{DynamicResource SecondaryButtonStyle}"
                                                        Padding="8,2" FontSize="{StaticResource FontSizeCaption}"
                                                        Margin="8,0,0,0" VerticalAlignment="Center"
                                                        ToolTip=""/>
                                            </StackPanel>
                                            <TextBlock x:Name="Mw_SettingsSshModeHint" Text="" Foreground="{DynamicResource TextTertiaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Grid Margin="0,12,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                                <TextBlock x:Name="Mw_SettingsAntiIdleLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBox Text="{Binding Settings.AntiIdleInterval}" Width="100" AutomationProperties.LabeledBy="{x:Reference Mw_SettingsAntiIdleLabel}"/>
                                                    <TextBlock Text="s" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" IsHitTestVisible="False"/>
                                                </StackPanel>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                                <TextBlock x:Name="Mw_SettingsSshTmoutLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBox Text="{Binding Settings.SshTmoutResetInterval}" Width="100" AutomationProperties.LabeledBy="{x:Reference Mw_SettingsSshTmoutLabel}"/>
                                                    <TextBlock Text="s" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" IsHitTestVisible="False"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                        <CheckBox x:Name="Mw_SettingsAutoOpenSftp" Content="" IsChecked="{Binding Settings.SftpAutoOpenOnSsh}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,12,0,0"/>
                                        <StackPanel Margin="0,12,0,0">
                                            <TextBlock x:Name="Mw_SettingsX11PathLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                            <DockPanel>
                                                <Button x:Name="Mw_SettingsBrowseX11Path" DockPanel.Dock="Right"
                                                        Padding="6,2" Margin="4,0,0,0" Click="OnBrowseX11PathClick"
                                                        Style="{DynamicResource SecondaryButtonStyle}"
                                                        FontSize="{StaticResource FontSizeCaption}"
                                                        AutomationProperties.Name="{loc:Translate A11yBrowseX11Path}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE838;" FontSize="12" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{loc:Translate BtnBrowse}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Button>
                                                <TextBox Text="{Binding Settings.X11ServerPath}" AutomationProperties.LabeledBy="{x:Reference Mw_SettingsX11PathLabel}"/>
                                            </DockPanel>
                                        </StackPanel>
                                        <CheckBox x:Name="Mw_SettingsX11AutoStart" Content="" IsChecked="{Binding Settings.X11AutoStart}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,12,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- SSH Gateways -->
                                <Border Padding="{StaticResource SpacingLg}" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource CornerRadiusLg}">
                                    <StackPanel>
                                        <DockPanel Margin="0,0,0,12">
                                            <TextBlock x:Name="Mw_SettingsGatewaysTitle" Text="" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                                                <Button x:Name="Mw_SettingsGatewaysAddBtn" Content="" Command="{Binding Settings.AddGatewayCommand}" Style="{DynamicResource PrimaryButtonStyle}" Padding="8,3" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,4,0"/>
                                                <Button x:Name="Mw_SettingsGatewaysEditBtn" Content="" Command="{Binding Settings.EditGatewayCommand}" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,3" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,4,0"/>
                                                <Button x:Name="Mw_SettingsGatewaysDeleteBtn" Content="" Command="{Binding Settings.DeleteGatewayCommand}" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,3" FontSize="{StaticResource FontSizeCaption}"/>
                                            </StackPanel>
                                        </DockPanel>
                                        <ListBox ItemsSource="{Binding Settings.Gateways}" SelectedItem="{Binding Settings.SelectedGateway}" MinHeight="80" MaxHeight="200" Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="4,2">
                                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,8,0" TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis">
                                                            <Run Text="{Binding User, Mode=OneWay}"/><Run Text="@"/><Run Text="{Binding Host, Mode=OneWay}"/><Run Text=":"/><Run Text="{Binding Port, Mode=OneWay}"/>
                                                        </TextBlock>
                                                        <TextBlock Text=" &#xE8D7;" FontFamily="Segoe MDL2 Assets" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource SshBadgeBrush}" VerticalAlignment="Center" Visibility="{Binding HasKey, Converter={StaticResource BoolToVisibilityConverter}}" AutomationProperties.Name=""/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- ═══════ RDP ═══════ -->
                    <TabItem x:Name="Mw_SettingsTabRdp" Style="{StaticResource ThemedTabItemStyle}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="{StaticResource SpacingLg}" MaxWidth="900">
                                <Border Padding="{StaticResource SpacingLg}" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource CornerRadiusLg}">
                                    <StackPanel>
                                        <TextBlock x:Name="Mw_SettingsRdpDefaultsTitle" Text="" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                                        <StackPanel Margin="0,0,0,8">
                                            <TextBlock x:Name="Mw_SettingsRdpModeLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                            <StackPanel Orientation="Horizontal">
                                                <ComboBox SelectedValue="{Binding Settings.RdpDefaultMode}" SelectedValuePath="Tag" Width="200" HorizontalAlignment="Left" AutomationProperties.LabeledBy="{x:Reference Mw_SettingsRdpModeLabel}">
                                                    <ComboBoxItem x:Name="Mw_RdpModeEmbedded" Content="" Tag="Embedded"/>
                                                    <ComboBoxItem x:Name="Mw_RdpModeExternal" Content="" Tag="External"/>
                                                </ComboBox>
                                                <Button x:Name="Mw_ApplyRdpModeAll"
                                                        Content=""
                                                        Command="{Binding Settings.ApplyRdpModeToAllCommand}"
                                                        Style="{DynamicResource SecondaryButtonStyle}"
                                                        Padding="8,2" FontSize="{StaticResource FontSizeCaption}"
                                                        Margin="8,0,0,0" VerticalAlignment="Center"
                                                        ToolTip=""/>
                                            </StackPanel>
                                            <TextBlock x:Name="Mw_SettingsRdpModeHint" Text="" Foreground="{DynamicResource TextTertiaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Grid Margin="0,8,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                                <TextBlock x:Name="Mw_SettingsRdpWidthLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBox Text="{Binding Settings.DefaultResolutionWidth}" Width="100" AutomationProperties.LabeledBy="{x:Reference Mw_SettingsRdpWidthLabel}"/>
                                                    <TextBlock Text="px" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" IsHitTestVisible="False"/>
                                                </StackPanel>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                                <TextBlock x:Name="Mw_SettingsRdpHeightLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBox Text="{Binding Settings.DefaultResolutionHeight}" Width="100" AutomationProperties.LabeledBy="{x:Reference Mw_SettingsRdpHeightLabel}"/>
                                                    <TextBlock Text="px" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" IsHitTestVisible="False"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                        <StackPanel Margin="0,8,0,0">
                                            <TextBlock x:Name="Mw_SettingsRdpColorDepthLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                            <ComboBox SelectedValue="{Binding Settings.RdpDefaultColorDepth}" SelectedValuePath="Tag" Width="120" HorizontalAlignment="Left" AutomationProperties.LabeledBy="{x:Reference Mw_SettingsRdpColorDepthLabel}">
                                                <ComboBoxItem x:Name="Mw_ColorDepth16" Content="" Tag="16"/>
                                                <ComboBoxItem x:Name="Mw_ColorDepth24" Content="" Tag="24"/>
                                                <ComboBoxItem x:Name="Mw_ColorDepth32" Content="" Tag="32"/>
                                            </ComboBox>
                                        </StackPanel>
                                        <StackPanel Margin="0,8,0,0">
                                            <TextBlock x:Name="Mw_SettingsRdpAudioLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                            <ComboBox SelectedValue="{Binding Settings.RdpDefaultAudioMode}" SelectedValuePath="Tag" Width="200" HorizontalAlignment="Left" AutomationProperties.LabeledBy="{x:Reference Mw_SettingsRdpAudioLabel}">
                                                <ComboBoxItem x:Name="Mw_SettingsRdpAudioDisabled" Content="" Tag="0"/>
                                                <ComboBoxItem x:Name="Mw_SettingsRdpAudioLocal" Content="" Tag="1"/>
                                                <ComboBoxItem x:Name="Mw_SettingsRdpAudioRemote" Content="" Tag="2"/>
                                            </ComboBox>
                                        </StackPanel>
                                        <CheckBox x:Name="Mw_SettingsRdpNla" Content="" IsChecked="{Binding Settings.RdpDefaultNla}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,12,0,0"/>
                                        <CheckBox x:Name="Mw_SettingsRdpDynRes" Content="" IsChecked="{Binding Settings.RdpDefaultDynamicResolution}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,6,0,0"/>
                                        <CheckBox x:Name="Mw_SettingsRdpMultiMon" Content="" IsChecked="{Binding Settings.RdpDefaultMultiMonitor}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,6,0,0"/>
                                        <CheckBox x:Name="Mw_SettingsRdpAutoReconnect" Content="" IsChecked="{Binding Settings.RdpDefaultAutoReconnect}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,6,0,0"/>
                                        <CheckBox x:Name="Mw_SettingsRdpBitmapCache" Content="" IsChecked="{Binding Settings.RdpDefaultBitmapCaching}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,6,0,0"/>
                                        <CheckBox x:Name="Mw_SettingsRdpCompression" Content="" IsChecked="{Binding Settings.RdpDefaultCompression}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,6,0,0"/>
                                        <TextBlock x:Name="Mw_SettingsRdpRedirTitle" Text="" FontSize="{StaticResource FontSizeBody}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,16,0,8"/>
                                        <CheckBox x:Name="Mw_SettingsRdpClipboard" Content="" IsChecked="{Binding Settings.RdpDefaultRedirectClipboard}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,6,0,0"/>
                                        <CheckBox x:Name="Mw_SettingsRdpDrives" Content="" IsChecked="{Binding Settings.RdpDefaultRedirectDrives}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,6,0,0"/>
                                        <CheckBox x:Name="Mw_SettingsRdpPrinters" Content="" IsChecked="{Binding Settings.RdpDefaultRedirectPrinters}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,6,0,0"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- ═══════ SECURITY ═══════ -->
                    <TabItem x:Name="Mw_SettingsTabSecurity" Style="{StaticResource ThemedTabItemStyle}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="{StaticResource SpacingLg}" MaxWidth="900">
                                <Border Padding="{StaticResource SpacingLg}" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource CornerRadiusLg}">
                                    <StackPanel>
                                        <TextBlock x:Name="Mw_SettingsCredProviderTitle" Text="" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                                        <CheckBox x:Name="Mw_SettingsCredProviderEnabled" Content="" IsChecked="{Binding Settings.UseExternalCredentialProvider}" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <StackPanel Margin="0,12,0,0" IsEnabled="{Binding Settings.UseExternalCredentialProvider}" AutomationProperties.HelpText="{Binding Settings.CredProvHelpText}">
                                            <TextBlock x:Name="Mw_SettingsCredProvPresetLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                            <ComboBox x:Name="Mw_SettingsCredProvPreset" SelectionChanged="OnCredProvPresetChanged" FontSize="{StaticResource FontSizeBody}" Margin="0,0,0,12"/>
                                        </StackPanel>
                                        <StackPanel Margin="0,12,0,0" IsEnabled="{Binding Settings.UseExternalCredentialProvider}" AutomationProperties.HelpText="{Binding Settings.CredProvHelpText}">
                                            <TextBlock x:Name="Mw_SettingsCredProviderCmdLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                            <TextBox x:Name="Mw_SettingsCredProvCmd" Text="{Binding Settings.CredentialProviderCommand}" Tag=""/>
                                            <TextBlock x:Name="Mw_SettingsCredProvCmdHint" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeSmallCaption}" FontFamily="Consolas" Margin="0,4,0,0"/>
                                        </StackPanel>
                                        <StackPanel Margin="0,12,0,0" IsEnabled="{Binding Settings.UseExternalCredentialProvider}" AutomationProperties.HelpText="{Binding Settings.CredProvHelpText}">
                                            <TextBlock x:Name="Mw_SettingsCredProviderDbLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                            <DockPanel>
                                                <Button x:Name="Mw_SettingsCredProvDbBrowseBtn" DockPanel.Dock="Right" Padding="6,2" Margin="4,0,0,0" Click="OnCredProvDbBrowseClick" Style="{DynamicResource SecondaryButtonStyle}" FontSize="{StaticResource FontSizeCaption}" AutomationProperties.Name="{loc:Translate A11yBrowseDatabase}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE838;" FontSize="12" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{loc:Translate BtnBrowse}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Button>
                                                <TextBox Text="{Binding Settings.CredentialProviderDatabase}" AutomationProperties.LabeledBy="{x:Reference Mw_SettingsCredProviderDbLabel}"/>
                                            </DockPanel>
                                        </StackPanel>
                                        <DockPanel Margin="0,16,0,0" IsEnabled="{Binding Settings.UseExternalCredentialProvider}" AutomationProperties.HelpText="{Binding Settings.CredProvHelpText}">
                                            <Button x:Name="Mw_SettingsCredProvTestBtn" DockPanel.Dock="Right" Command="{Binding Settings.TestCredentialProviderCommand}" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,4" FontSize="{StaticResource FontSizeCaption}"/>
                                            <TextBlock x:Name="Mw_SettingsCredProvTestResult" Text="{Binding Settings.CredentialProviderTestResult}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" FontStyle="Italic" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                        </DockPanel>
                                        <CheckBox x:Name="Mw_SettingsCredGuard" Content="" IsChecked="{Binding Settings.RequireCredentialGuard}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,16,0,0"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- ═══════ ADVANCED ═══════ -->
                    <TabItem x:Name="Mw_SettingsTabAdvanced" Style="{StaticResource ThemedTabItemStyle}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="{StaticResource SpacingLg}" MaxWidth="900">
                                <!-- Logging -->
                                <Border Padding="{StaticResource SpacingLg}" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource CornerRadiusLg}" Margin="0,0,0,16">
                                    <StackPanel>
                                        <TextBlock x:Name="Mw_SettingsSessionLoggingTitle" Text="" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                                        <CheckBox x:Name="Mw_SettingsEnableLogging" Content="" IsChecked="{Binding Settings.EnableLogging}" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <CheckBox x:Name="Mw_SettingsSessionLoggingEnabled" Content="" IsChecked="{Binding Settings.SessionLoggingEnabled}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,8,0,0"/>
                                        <StackPanel Margin="0,12,0,0" IsEnabled="{Binding Settings.SessionLoggingEnabled}">
                                            <TextBlock x:Name="Mw_SettingsSessionLogDirLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                            <DockPanel>
                                                <Button x:Name="Mw_SettingsBrowseSessionLogDir" DockPanel.Dock="Right"
                                                        Padding="6,2" Margin="4,0,0,0" Click="OnBrowseSessionLogDirClick"
                                                        Style="{DynamicResource SecondaryButtonStyle}"
                                                        FontSize="{StaticResource FontSizeCaption}"
                                                        AutomationProperties.Name="{loc:Translate A11yBrowseSessionLogDir}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE838;" FontSize="12" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{loc:Translate BtnBrowse}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Button>
                                                <TextBox Text="{Binding Settings.SessionLogDirectory}"/>
                                            </DockPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <!-- Timeouts -->
                                <Border Padding="{StaticResource SpacingLg}" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource CornerRadiusLg}" Margin="0,0,0,16">
                                    <StackPanel>
                                        <TextBlock x:Name="Mw_SettingsTimeoutsTitle" Text="" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                                <TextBlock x:Name="Mw_SettingsTunnelDelayLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBox Text="{Binding Settings.TunnelEstablishmentDelayMs}" Width="100"/>
                                                    <TextBlock Text="ms" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" IsHitTestVisible="False"/>
                                                </StackPanel>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                                <TextBlock x:Name="Mw_SettingsRdpTimeoutLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBox Text="{Binding Settings.EmbeddedRdpTimeoutMs}" Width="100"/>
                                                    <TextBlock Text="ms" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" IsHitTestVisible="False"/>
                                                </StackPanel>
                                            </StackPanel>
                                            <StackPanel Margin="0,12,0,0">
                                                <TextBlock x:Name="Mw_SettingsExtToolTimeoutLabel" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,0,4"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBox Text="{Binding Settings.ExternalToolTimeoutMs}" Width="100"/>
                                                    <TextBlock Text="ms" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" IsHitTestVisible="False"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <!-- External Tools -->
                                <!-- Third-Party Tool Detection (NirSoft / Sysinternals / NanaRun) -->
                                <Border Padding="{StaticResource SpacingLg}" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource CornerRadiusLg}" Margin="0,0,0,16">
                                    <StackPanel>
                                        <TextBlock x:Name="Mw_SettingsExtProvTitle" Text="" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4"/>
                                        <TextBlock x:Name="Mw_SettingsExtProvDesc" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}" TextWrapping="Wrap" Margin="0,0,0,12"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="130"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock x:Name="Mw_SettingsLblSysintPath" Grid.Row="0" Grid.Column="0" Text="" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"/>
                                            <TextBox x:Name="Mw_SettingsTxtSysintPath" Grid.Row="0" Grid.Column="1" Text="{Binding Settings.SysinternalsPath, UpdateSourceTrigger=LostFocus}" Margin="0,0,0,8" FontSize="{StaticResource FontSizeBody}"/>
                                            <Button x:Name="Mw_SettingsBtnSysintBrowse" Grid.Row="0" Grid.Column="2" Padding="6,2" Margin="4,0,0,8" Click="OnSysinternalsPathBrowseClick" Style="{DynamicResource SecondaryButtonStyle}" FontSize="{StaticResource FontSizeCaption}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE838;" FontSize="12" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{loc:Translate BtnBrowse}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Button>
                                            <TextBlock x:Name="Mw_SettingsLblNirSoftPath" Grid.Row="1" Grid.Column="0" Text="" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"/>
                                            <TextBox x:Name="Mw_SettingsTxtNirSoftPath" Grid.Row="1" Grid.Column="1" Text="{Binding Settings.NirSoftPath, UpdateSourceTrigger=LostFocus}" Margin="0,0,0,8" FontSize="{StaticResource FontSizeBody}"/>
                                            <Button x:Name="Mw_SettingsBtnNirSoftBrowse" Grid.Row="1" Grid.Column="2" Padding="6,2" Margin="4,0,0,8" Click="OnNirSoftPathBrowseClick" Style="{DynamicResource SecondaryButtonStyle}" FontSize="{StaticResource FontSizeCaption}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE838;" FontSize="12" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{loc:Translate BtnBrowse}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Button>
                                            <TextBlock x:Name="Mw_SettingsLblNanaRunPath" Grid.Row="2" Grid.Column="0" Text="" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"/>
                                            <TextBox x:Name="Mw_SettingsTxtNanaRunPath" Grid.Row="2" Grid.Column="1" Text="{Binding Settings.NanaRunPath, UpdateSourceTrigger=LostFocus}" Margin="0,0,0,0" FontSize="{StaticResource FontSizeBody}"/>
                                            <Button x:Name="Mw_SettingsBtnNanaRunBrowse" Grid.Row="2" Grid.Column="2" Padding="6,2" Margin="4,0,0,0" Click="OnNanaRunPathBrowseClick" Style="{DynamicResource SecondaryButtonStyle}" FontSize="{StaticResource FontSizeCaption}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE838;" FontSize="12" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{loc:Translate BtnBrowse}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Button>
                                        </Grid>
                                        <DockPanel Margin="0,8,0,0">
                                            <Button x:Name="Mw_SettingsBtnRescan" DockPanel.Dock="Right" Content="" Click="OnRescanExternalToolsClick" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,3" FontSize="{StaticResource FontSizeCaption}"/>
                                            <TextBlock x:Name="Mw_SettingsExtProvStatus" Text="" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" FontStyle="Italic" VerticalAlignment="Center"/>
                                        </DockPanel>
                                    </StackPanel>
                                </Border>

                                <!-- Command Library Git Sync -->
                                <Border Padding="{StaticResource SpacingLg}" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource CornerRadiusLg}" Margin="0,0,0,16">
                                    <StackPanel>
                                        <TextBlock x:Name="Mw_SettingsCmdLibSyncTitle" Text="" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                                        <CheckBox x:Name="Mw_SettingsCmdLibSyncEnable" Content="" IsChecked="{Binding Settings.CmdLibGitSyncEnabled}" Margin="0,0,0,8" FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <Grid Margin="0,4,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="130"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock x:Name="Mw_SettingsCmdLibSyncLblUrl" Grid.Row="0" Grid.Column="0" Text="" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"/>
                                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Settings.CmdLibGitSyncUrl, UpdateSourceTrigger=LostFocus}" Margin="0,0,0,8" FontSize="{StaticResource FontSizeBody}"/>
                                            <TextBlock x:Name="Mw_SettingsCmdLibSyncLblBranch" Grid.Row="1" Grid.Column="0" Text="" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"/>
                                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Settings.CmdLibGitSyncBranch, UpdateSourceTrigger=LostFocus}" Margin="0,0,0,8" FontSize="{StaticResource FontSizeBody}"/>
                                            <TextBlock x:Name="Mw_SettingsCmdLibSyncLblToken" Grid.Row="2" Grid.Column="0" Text="" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"/>
                                            <DockPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,8">
                                                <Button x:Name="Mw_SettingsCmdLibSyncTokenClear" DockPanel.Dock="Right" Content="&#xE711;" FontFamily="Segoe MDL2 Assets" Click="OnCmdLibSyncTokenClear" Style="{DynamicResource ToolbarGhostButtonStyle}" FontSize="12" Width="24" Height="24" Margin="4,0,0,0" Visibility="Collapsed"/>
                                                <TextBlock x:Name="Mw_SettingsCmdLibSyncTokenStatus" DockPanel.Dock="Right" VerticalAlignment="Center" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource SuccessBrush}" Margin="8,0,0,0"/>
                                                <PasswordBox x:Name="Mw_SettingsCmdLibSyncToken" FontSize="{StaticResource FontSizeBody}" PasswordChanged="OnCmdLibSyncTokenChanged"/>
                                            </DockPanel>
                                            <TextBlock x:Name="Mw_SettingsCmdLibSyncLblAuthor" Grid.Row="3" Grid.Column="0" Text="" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"/>
                                            <DockPanel Grid.Row="3" Grid.Column="1" Margin="0,0,0,8">
                                                <TextBox Text="{Binding Settings.CmdLibGitSyncAuthorEmail, UpdateSourceTrigger=LostFocus}" DockPanel.Dock="Right" Width="180" FontSize="{StaticResource FontSizeBody}"/>
                                                <TextBox Text="{Binding Settings.CmdLibGitSyncAuthorName, UpdateSourceTrigger=LostFocus}" Margin="0,0,8,0" FontSize="{StaticResource FontSizeBody}"/>
                                            </DockPanel>
                                        </Grid>
                                        <CheckBox x:Name="Mw_SettingsCmdLibSyncOnStartup" Content="" IsChecked="{Binding Settings.CmdLibGitSyncOnStartup}" Margin="0,4,0,4" FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <CheckBox x:Name="Mw_SettingsCmdLibSyncAutoPush" Content="" IsChecked="{Binding Settings.CmdLibGitSyncAutoPush}" Margin="0,0,0,8" FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <Button x:Name="Mw_SettingsCmdLibSyncTestBtn" Content="" Click="OnCmdLibSyncTestClick" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,3" FontSize="{StaticResource FontSizeCaption}" HorizontalAlignment="Left"/>
                                    </StackPanel>
                                </Border>

                                <Border Padding="{StaticResource SpacingLg}" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource CornerRadiusLg}">
                                    <StackPanel>
                                        <DockPanel Margin="0,0,0,12">
                                            <TextBlock x:Name="Mw_SettingsExtToolsTitle" Text="" FontSize="{StaticResource FontSizeSubtitle}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                                                <Button x:Name="Mw_SettingsExtToolsAddBtn" Content="" Command="{Binding Settings.AddExternalToolCommand}" Style="{DynamicResource PrimaryButtonStyle}" Padding="8,3" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,4,0"/>
                                                <Button x:Name="Mw_SettingsExtToolsRemoveBtn" Content="" Command="{Binding Settings.RemoveExternalToolCommand}" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,3" FontSize="{StaticResource FontSizeCaption}"/>
                                            </StackPanel>
                                        </DockPanel>
                                        <ListBox ItemsSource="{Binding Settings.ExternalTools}" SelectedItem="{Binding Settings.SelectedExternalTool}" MinHeight="60" MaxHeight="120" Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="4,2">
                                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,8,0" TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Text="{Binding ExecutablePath}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeCaption}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>

                                        <!-- Inline edit panel for selected tool -->
                                        <Border Margin="0,12,0,0" Padding="{StaticResource SpacingMd}" Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="{StaticResource CornerRadiusMd}"
                                                DataContext="{Binding Settings.SelectedExternalTool}"
                                                Visibility="{Binding DataContext.Settings.SelectedExternalTool, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource NullToVisibilityConverter}}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <TextBlock x:Name="Mw_ExtToolLblName" Grid.Row="0" Grid.Column="0" Text="" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"/>
                                                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" FontSize="{StaticResource FontSizeBody}"/>

                                                <TextBlock x:Name="Mw_ExtToolLblPath" Grid.Row="1" Grid.Column="0" Text="" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"/>
                                                <DockPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,6">
                                                    <Button x:Name="Mw_ExtToolBrowseBtn" DockPanel.Dock="Right"
                                                            Padding="6,2" Margin="4,0,0,0" Click="OnExtToolBrowseClick"
                                                            Style="{DynamicResource SecondaryButtonStyle}"
                                                            FontSize="{StaticResource FontSizeCaption}"
                                                            AutomationProperties.Name="{loc:Translate A11yBrowseExecutable}">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE838;" FontSize="12" VerticalAlignment="Center"/>
                                                            <TextBlock Text="{loc:Translate BtnBrowse}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                    </Button>
                                                    <TextBox Text="{Binding ExecutablePath, UpdateSourceTrigger=PropertyChanged}" FontSize="{StaticResource FontSizeBody}"/>
                                                </DockPanel>

                                                <TextBlock x:Name="Mw_ExtToolLblArgs" Grid.Row="2" Grid.Column="0" Text="" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"/>
                                                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Arguments, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" FontSize="{StaticResource FontSizeBody}" FontFamily="Consolas"/>

                                                <TextBlock x:Name="Mw_ExtToolLblWorkDir" Grid.Row="3" Grid.Column="0" Text="" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{StaticResource FontSizeBody}"/>
                                                <DockPanel Grid.Row="3" Grid.Column="1" Margin="0,0,0,6">
                                                    <Button x:Name="Mw_ExtToolWorkDirBrowseBtn" DockPanel.Dock="Right"
                                                            Padding="6,2" Margin="4,0,0,0" Click="OnExtToolWorkDirBrowseClick"
                                                            Style="{DynamicResource SecondaryButtonStyle}"
                                                            FontSize="{StaticResource FontSizeCaption}"
                                                            AutomationProperties.Name="{loc:Translate A11yBrowseWorkDir}">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xED25;" FontSize="12" VerticalAlignment="Center"/>
                                                            <TextBlock Text="{loc:Translate BtnBrowse}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                    </Button>
                                                    <TextBox Text="{Binding WorkingDirectory, UpdateSourceTrigger=PropertyChanged}" FontSize="{StaticResource FontSizeBody}"/>
                                                </DockPanel>

                                                <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="0,2,0,6">
                                                    <CheckBox x:Name="Mw_ExtToolChkAdmin" Content="" IsChecked="{Binding RunAsAdministrator}" Margin="0,0,16,0" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="{StaticResource FontSizeBody}"/>
                                                    <CheckBox x:Name="Mw_ExtToolChkHidden" Content="" IsChecked="{Binding RunHidden}" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="{StaticResource FontSizeBody}"/>
                                                </StackPanel>

                                                <ItemsControl x:Name="Mw_ExtToolPlaceholderList" Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,2,0,0">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel Orientation="Horizontal"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>

                                                <!-- Preview + Test -->
                                                <DockPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,8,0,0">
                                                    <Button x:Name="Mw_ExtToolTestBtn" DockPanel.Dock="Right" Click="OnExtToolTestClick" Style="{DynamicResource SecondaryButtonStyle}" Padding="8,3" FontSize="{StaticResource FontSizeCaption}" Margin="4,0,0,0"/>
                                                    <TextBlock x:Name="Mw_ExtToolPreview" Text="" FontFamily="Consolas" FontSize="{StaticResource FontSizeSmallCaption}" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                                                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                                                </DockPanel>
                                            </Grid>
                                        </Border>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                </TabControl>
            </Grid>

            <!-- ============================================================ -->
            <!-- ABOUT TAB: Application info                                   -->
            <!-- ============================================================ -->
            <ScrollViewer Visibility="{Binding IsAboutTabSelected, Converter={StaticResource BoolToVisibilityConverter}}"
                          VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="{StaticResource SpacingXl}" MaxWidth="500"
                            HorizontalAlignment="Center" VerticalAlignment="Center">

                    <Border Padding="28"
                            Background="{DynamicResource CardBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource CornerRadiusXl}">
                        <StackPanel>
                            <!-- Logo + Title -->
                            <StackPanel HorizontalAlignment="Center" Margin="0,0,0,20">
                                <TextBlock Text="&#xE7F4;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="{StaticResource IconSizeHero}"
                                           Foreground="{DynamicResource AccentBrush}"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,10"
                                           AutomationProperties.Name=""/>
                                <TextBlock x:Name="Mw_AboutAppName" Text=""
                                           FontSize="{StaticResource FontSizeHeadline}" FontWeight="Bold"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           HorizontalAlignment="Center"/>
                                <TextBlock x:Name="AboutVersionText"
                                           FontSize="{StaticResource FontSizeBodyLarge}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           HorizontalAlignment="Center"
                                           Margin="0,4,0,0"/>
                                <TextBlock x:Name="Mw_AboutTagline" Text=""
                                           FontSize="{StaticResource FontSizeBody}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           HorizontalAlignment="Center"
                                           Margin="0,8,0,0"/>
                            </StackPanel>

                            <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="0,0,0,16"/>

                            <!-- Info grid -->
                            <Grid HorizontalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock x:Name="Mw_AboutAuthorLabel" Grid.Row="0" Grid.Column="0" Text=""
                                           FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="Julien Bombled"
                                           FontSize="{StaticResource FontSizeBody}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                                <TextBlock x:Name="Mw_AboutLicenseLabel" Grid.Row="1" Grid.Column="0" Text=""
                                           FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                                <TextBlock x:Name="Mw_AboutLicenseValue" Grid.Row="1" Grid.Column="1" Text=""
                                           FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                                <TextBlock x:Name="Mw_AboutRuntimeLabel" Grid.Row="2" Grid.Column="0" Text=""
                                           FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                                <TextBlock x:Name="AboutRuntimeText"
                                           Grid.Row="2" Grid.Column="1"
                                           FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                                <TextBlock x:Name="Mw_AboutPlatformLabel" Grid.Row="3" Grid.Column="0" Text=""
                                           FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                                <TextBlock x:Name="AboutPlatformText"
                                           Grid.Row="3" Grid.Column="1"
                                           FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                            </Grid>

                            <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="0,16,0,16"/>

                            <!-- Features -->
                            <TextBlock x:Name="Mw_AboutFeaturesTitle" Text="" FontSize="{StaticResource FontSizeBodyLarge}" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,8"/>
                            <StackPanel Margin="8,0,0,0">
                                <TextBlock x:Name="Mw_AboutFeature1" FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,3"
                                           Text=""/>
                                <TextBlock x:Name="Mw_AboutFeature2" FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,3"
                                           Text=""/>
                                <TextBlock x:Name="Mw_AboutFeature3" FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,3"
                                           Text=""/>
                                <TextBlock x:Name="Mw_AboutFeature4" FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,3"
                                           Text=""/>
                                <TextBlock x:Name="Mw_AboutFeature5" FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,3"
                                           Text=""/>
                                <TextBlock x:Name="Mw_AboutFeature6" FontSize="{StaticResource FontSizeBody}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,3"
                                           Text=""/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Sessions overlay removed — now inside Servers Grid Column 2 -->
        </Grid>

        <!-- FULLSCREEN EXIT ICON (small icon top-right, hidden by default) -->
        <Button x:Name="FullscreenBar"
                Grid.Row="0" Grid.RowSpan="3"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,2,4,0"
                Width="24" Height="24"
                Content="⛶"
                FontSize="{StaticResource FontSizeSubtitle}"
                Click="OnExitFullscreenClick"
                Visibility="Collapsed"
                Panel.ZIndex="999"
                Opacity="0.6"
                Cursor="Hand"
                AutomationProperties.Name="{loc:Translate A11yExitFullscreen}">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="{StaticResource CornerRadiusSm}" Padding="2">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                        <Setter Property="Opacity" Value="1"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>

        <!-- STATUS BAR -->
        <Border Grid.Row="2"
                Background="{DynamicResource SurfaceBrush}"
                BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource BorderBrush}"
                AutomationProperties.Name="Status">
            <DockPanel Margin="8,0">
                <TextBlock Text="{Binding StatusText}"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           VerticalAlignment="Center" FontSize="{StaticResource FontSizeCaption}"
                           AutomationProperties.LiveSetting="Polite"/>
                <!-- Subtle F1 shortcut hint — visible when status shows default ready text -->
                <TextBlock x:Name="Mw_StatusBarShortcutHint" Text=""
                           FontSize="{StaticResource FontSizeSmallCaption}"
                           Foreground="{DynamicResource TextDisabledBrush}"
                           VerticalAlignment="Center"
                           Margin="12,0,0,0"/>
                <StackPanel Orientation="Horizontal"
                            DockPanel.Dock="Right"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <!-- File sharing status -->
                    <TextBlock x:Name="Mw_SharingStatus"
                               FontSize="{StaticResource FontSizeCaption}" Margin="0,0,12,0"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource SuccessTextBrush}"
                               Visibility="Collapsed"/>
                    <!-- Tunnel panel toggle badge -->
                    <Button x:Name="Mw_StatusTunnelToggle" Command="{Binding ToggleTunnelPanelCommand}"
                            Padding="4,2" FontSize="{StaticResource FontSizeCaption}" Margin="0,0,8,0"
                            VerticalAlignment="Center"
                            Style="{DynamicResource ToolbarGhostButtonStyle}"
                            AutomationProperties.Name="{loc:Translate A11yToggleTunnelPanel}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE945;" FontFamily="Segoe MDL2 Assets"
                                           FontSize="{StaticResource FontSizeCaption}" VerticalAlignment="Center" Margin="0,0,3,0"
                                           AutomationProperties.Name=""/>
                                <TextBlock VerticalAlignment="Center">
                                    <Run Text="{Binding TunnelCount, Mode=OneWay}"/>
                                </TextBlock>
                            </StackPanel>
                        </Button.Content>
                    </Button>
                    <!-- Broadcast mode toggle -->
                    <Button x:Name="Mw_BroadcastToggleBtn" Command="{Binding ToggleBroadcastCommand}"
                            ToolTip="{Binding BroadcastToggleTooltip}"
                            Padding="4,2" FontSize="{StaticResource FontSizeSubtitle}" Margin="0,0,8,0"
                            VerticalAlignment="Center"
                            AutomationProperties.Name="{loc:Translate A11yToggleBroadcast}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE704;" FontFamily="Segoe MDL2 Assets" FontSize="{StaticResource FontSizeCaption}"
                                           VerticalAlignment="Center" Margin="0,0,3,0"
                                           AutomationProperties.Name=""/>
                                <TextBlock x:Name="Mw_BroadcastLabel" Text="" FontWeight="SemiBold" FontSize="{StaticResource FontSizeCaption}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button.Content>
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ToolbarGhostButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsBroadcastMode}" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource BroadcastActiveBrush}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource ErrorTextBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <TextBlock FontSize="{StaticResource FontSizeCaption}"
                               Foreground="{DynamicResource TextSecondaryBrush}">
                        <Run Text="{Binding ServerCount, Mode=OneWay}"/>
                        <Run x:Name="Mw_StatusBarServersLabel" Text=""/>
                        <Run Text="{Binding TunnelCount, Mode=OneWay}"/>
                        <Run x:Name="Mw_StatusBarTunnelsLabel" Text=""/>
                    </TextBlock>
                </StackPanel>
            </DockPanel>
        </Border>
        <!-- Quick Connect Command Palette (Ctrl+K) — uses a Popup so it gets its own
             HWND and renders above WindowsFormsHost / ActiveX surfaces (airspace fix). -->
        <Popup x:Name="CommandPalettePopup"
               IsOpen="{Binding IsCommandPaletteOpen}"
               StaysOpen="True"
               AllowsTransparency="True"
               Placement="Center"
               PlacementTarget="{Binding ElementName=MainContentArea}"
               Opened="OnCommandPaletteOpened">
            <Border MinWidth="550"
                    MaxWidth="700"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1" CornerRadius="{StaticResource CornerRadiusLg}"
                    PreviewKeyDown="OnPaletteKeyDown"
                    MouseDown="OnPaletteBorderClick">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="24" ShadowDepth="8" Opacity="0.4" Direction="270"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock x:Name="PaletteModeLabel"
                               Text=""
                               FontSize="{StaticResource FontSizeCaption}"
                               Foreground="{DynamicResource AccentBrush}"
                               FontWeight="SemiBold"
                               Visibility="Collapsed"
                               Margin="14,8,14,0"/>
                    <TextBox x:Name="PaletteInput"
                             Text="{Binding PaletteSearchText, UpdateSourceTrigger=PropertyChanged}"
                             Tag="{Binding PalettePlaceholder}"
                             Style="{StaticResource SearchTextBoxStyle}"
                             FontSize="{StaticResource FontSizeSubtitle}" Padding="14,10"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <Border Height="1" Background="{DynamicResource BorderBrush}"/>
                    <ListBox x:Name="PaletteResultsList"
                             ItemsSource="{Binding PaletteResults}"
                             SelectedItem="{Binding SelectedPaletteItem}"
                             MaxHeight="450"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             MouseDoubleClick="OnPaletteItemDoubleClick">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <DockPanel Margin="10,6">
                                    <Border DockPanel.Dock="Left"
                                            Width="3"
                                            CornerRadius="1.5"
                                            Margin="0,2,8,2"
                                            VerticalAlignment="Stretch">
                                        <Border.Background>
                                            <MultiBinding Converter="{StaticResource PaletteActiveIndicatorConverter}">
                                                <Binding Path="ConnectionType"/>
                                                <Binding Path="ConnectionState"/>
                                            </MultiBinding>
                                        </Border.Background>
                                    </Border>

                                    <Grid DockPanel.Dock="Right"
                                          Margin="8,0,0,0"
                                          VerticalAlignment="Center">
                                        <Border CornerRadius="{StaticResource CornerRadiusXs}"
                                                Opacity="0.15">
                                            <Border.Background>
                                                <MultiBinding Converter="{StaticResource ConnectionTypeToBrushConverter}">
                                                    <Binding Path="ConnectionType"/>
                                                    <Binding Path="DataContext.ThemeRevision" ElementName="MainWindowRoot"/>
                                                </MultiBinding>
                                            </Border.Background>
                                        </Border>
                                        <TextBlock Margin="6,2"
                                                   Text="{Binding ConnectionTypeBadge}"
                                                   FontSize="{StaticResource FontSizeSmallCaption}"
                                                   FontWeight="SemiBold">
                                            <TextBlock.Foreground>
                                                <MultiBinding Converter="{StaticResource ConnectionTypeToBrushConverter}">
                                                    <Binding Path="ConnectionType"/>
                                                    <Binding Path="DataContext.ThemeRevision" ElementName="MainWindowRoot"/>
                                                </MultiBinding>
                                            </TextBlock.Foreground>
                                        </TextBlock>
                                    </Grid>

                                    <StackPanel>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Path Grid.Column="0"
                                                  Width="14"
                                                  Height="14"
                                                  Stretch="Uniform"
                                                  Data="{Binding ConnectionType, Converter={StaticResource TypeToGeoConverter}}"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,6,0">
                                                <Path.Fill>
                                                    <MultiBinding Converter="{StaticResource TypeToColorConverter}">
                                                        <Binding Path="ConnectionType"/>
                                                        <Binding Path="DataContext.ThemeRevision" ElementName="MainWindowRoot"/>
                                                    </MultiBinding>
                                                </Path.Fill>
                                            </Path>
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding DisplayName}"
                                                       FontSize="{StaticResource FontSizeBody}"
                                                       FontWeight="Medium"
                                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                                       VerticalAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </Grid>

                                        <StackPanel Orientation="Horizontal"
                                                    Margin="20,2,0,0">
                                            <TextBlock Text="{Binding Endpoint}"
                                                       FontSize="{StaticResource FontSizeCaption}"
                                                       Foreground="{DynamicResource TextDisabledBrush}"
                                                       Visibility="{Binding Endpoint, Converter={StaticResource NullToVisibilityConverter}}"
                                                       TextTrimming="CharacterEllipsis"
                                                       MaxWidth="220"/>
                                            <TextBlock Text="{Binding Username, StringFormat={}  ·  {0}}"
                                                       FontSize="{StaticResource FontSizeCaption}"
                                                       Foreground="{DynamicResource TextDisabledBrush}"
                                                       Visibility="{Binding Username, Converter={StaticResource NullToVisibilityConverter}}"/>
                                            <TextBlock Text="{Binding ProjectName, StringFormat={}  ·  {0}}"
                                                       FontSize="{StaticResource FontSizeCaption}"
                                                       Foreground="{DynamicResource AccentBrush}"
                                                       Visibility="{Binding ProjectName, Converter={StaticResource NullToVisibilityConverter}}"/>
                                            <TextBlock Text="{Binding Group, StringFormat={}  ·  {0}}"
                                                       FontSize="{StaticResource FontSizeCaption}"
                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                       Visibility="{Binding Group, Converter={StaticResource NullToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DockPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="2,0"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Bd"
                                                    Background="{TemplateBinding Background}"
                                                    CornerRadius="{StaticResource CornerRadiusSm}"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background"
                                                            Value="{DynamicResource CardBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background"
                                                            Value="{DynamicResource HighlightBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                    <TextBlock x:Name="Mw_PaletteNoResults" Text=""
                               FontSize="{StaticResource FontSizeBody}" Padding="14,10"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               Visibility="{Binding HasNoPaletteResults, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <TextBlock x:Name="Mw_PaletteHints" Text=""
                               FontSize="{StaticResource FontSizeCaption}" Padding="14,6"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </Border>
        </Popup>

        <!-- ============================================================ -->
        <!-- ONBOARDING OVERLAY (first launch)                             -->
        <!-- ============================================================ -->
        <Border x:Name="OnboardingOverlay"
                Grid.Row="0" Grid.RowSpan="3"
                Panel.ZIndex="500"
                Focusable="True"
                Visibility="{Binding IsVisible, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}"
                Background="{DynamicResource OverlayBackground}"
                AutomationProperties.Name="{loc:Translate A11yOnboardingOverlay}">
            <Border.InputBindings>
                <KeyBinding Key="Escape" Command="{Binding EscapeCommand}"/>
            </Border.InputBindings>
            <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                    MinWidth="480" MaxWidth="560"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource CornerRadiusXl}"
                    Padding="32,24">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="32" ShadowDepth="12" Opacity="0.3" Direction="270"/>
                </Border.Effect>
                <StackPanel>
                    <!-- Step indicator dots -->
                    <ItemsControl ItemsSource="{Binding StepIndicatorStates}"
                                  HorizontalAlignment="Center" Margin="0,0,0,20">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Ellipse Width="8" Height="8" Margin="4,0">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Setter Property="Fill" Value="{DynamicResource TextDisabledBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding}" Value="True">
                                                    <Setter Property="Fill" Value="{DynamicResource AccentBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Step content (swapped via VM) -->
                    <TextBlock Text="{Binding TitleText}"
                               FontSize="{StaticResource FontSizeHeadline}" FontWeight="Bold"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               HorizontalAlignment="Center" TextAlignment="Center"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding SubtitleText}"
                               FontSize="{StaticResource FontSizeBodyLarge}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               HorizontalAlignment="Center" TextAlignment="Center"
                               TextWrapping="Wrap"
                               Margin="0,0,0,24"/>

                    <!-- Navigation buttons -->
                    <DockPanel HorizontalAlignment="Stretch"
                               KeyboardNavigation.TabNavigation="Cycle">
                        <Button Content="{Binding SkipLabel}"
                                Command="{Binding SkipCommand}"
                                AutomationProperties.Name="{Binding SkipLabel}"
                                Style="{DynamicResource ToolbarGhostButtonStyle}"
                                Padding="12,6" FontSize="{StaticResource FontSizeBody}"
                                DockPanel.Dock="Left"
                                Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <Button x:Name="OnboardingNextBtn"
                                Content="{Binding NextLabel}"
                                Command="{Binding NextCommand}"
                                AutomationProperties.Name="{Binding NextLabel}"
                                Style="{DynamicResource PrimaryButtonStyle}"
                                Padding="20,8" FontSize="{StaticResource FontSizeBody}"
                                HorizontalAlignment="Right"/>
                    </DockPanel>
                </StackPanel>
            </Border>
        </Border>

    </Grid>
</Window>
