<Window x:Class="Heimdall.App.Views.Dialogs.ServerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:Heimdall.App.Converters"
        xmlns:vm="clr-namespace:Heimdall.App.ViewModels.Dialogs"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        d:DataContext="{d:DesignInstance Type=vm:ServerDialogViewModel}"
        mc:Ignorable="d"
        Title="{Binding DialogTitle}"
        Width="650"
        Height="750"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Closing="OnWindowClosing">

    <Window.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="SectionDescriptionStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>

        <Style x:Key="HintTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>

        <Style x:Key="SectionCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="InsetCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
        </Style>

        <Style x:Key="DiagramNodeStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Width" Value="165"/>
            <Setter Property="MinHeight" Value="86"/>
        </Style>

        <Style x:Key="ArrowCaptionStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="FormTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="FormComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="FormCheckBoxStyle" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="FormPasswordBoxStyle" TargetType="PasswordBox" BasedOn="{StaticResource {x:Type PasswordBox}}">
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="RdpOnlySectionStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ConnectionType}" Value="RDP">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SshOnlySectionStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ConnectionType}" Value="SSH">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SshFamilySectionStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ConnectionType}" Value="SSH">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ConnectionType}" Value="SFTP">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Gateway/routing sections: hidden for Local shell, Citrix, and VNC -->
        <Style x:Key="NetworkSectionStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ConnectionType}" Value="VNC">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ConnectionType}" Value="FTP">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GatewaySectionStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding UsesGateway}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DirectSectionStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding UsesGateway}" Value="False">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ManualTunnelPortStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding UsesGateway}" Value="True"/>
                        <Condition Binding="{Binding UseAutomaticTunnelPort}" Value="False"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Visibility" Value="Visible"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0" Style="{DynamicResource ThemedTabControlStyle}">
            <TabItem x:Name="DlgSrv_TabConnection" Style="{DynamicResource ThemedTabItemStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <Border Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_ConnectionBasicsTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_ConnectionBasicsDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <Grid Margin="0,16,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="150"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock x:Name="DlgSrv_DisplayNameLabel" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock x:Name="DlgSrv_ConnectionTypeLabel" Grid.Row="0" Grid.Column="1" Style="{StaticResource LabelStyle}" Margin="12,0,0,4"/>
                                    <TextBox Grid.Row="1" Grid.Column="0"
                                             Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource FormTextBoxStyle}"/>
                                    <ComboBox Grid.Row="1" Grid.Column="1"
                                              Margin="12,0,0,0"
                                              SelectedValue="{Binding ConnectionType}"
                                              SelectedValuePath="Content"
                                              Style="{StaticResource FormComboBoxStyle}">
                                        <ComboBoxItem Content="RDP"/>
                                        <ComboBoxItem Content="SSH"/>
                                        <ComboBoxItem Content="SFTP"/>
                                        <ComboBoxItem Content="FTP"/>
                                        <ComboBoxItem Content="Local"/>
                                        <ComboBoxItem Content="Citrix"/>
                                        <ComboBoxItem Content="VNC"/>
                                        <ComboBoxItem Content="Telnet"/>
                                    </ComboBox>
                                    <!-- Server/Port row: hidden for Local shell and Citrix -->
                                    <TextBlock x:Name="DlgSrv_ServerLabel" Grid.Row="2" Grid.Column="0" Margin="0,14,0,4">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock" BasedOn="{StaticResource LabelStyle}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding EndpointPortLabel}" Margin="12,14,0,4">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock" BasedOn="{StaticResource LabelStyle}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBox Grid.Row="3" Grid.Column="0"
                                             Text="{Binding RemoteServer, UpdateSourceTrigger=PropertyChanged}">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox" BasedOn="{StaticResource FormTextBoxStyle}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                    <TextBox Grid.Row="3" Grid.Column="1"
                                             Margin="12,0,0,0"
                                             Width="120"
                                             HorizontalAlignment="Left"
                                             Text="{Binding EndpointPort, UpdateSourceTrigger=PropertyChanged}">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox" BasedOn="{StaticResource FormTextBoxStyle}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </Grid>
                                <TextBlock Text="{Binding EndpointPortHelpText}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource HintTextStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_GatewayRoutingTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_GatewayRoutingDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <CheckBox x:Name="DlgSrv_DirectConnectCb"
                                          IsChecked="{Binding DirectConnection}"
                                          Style="{StaticResource FormCheckBoxStyle}"
                                          Margin="0,14,0,0"/>
                                <Grid Margin="0,12,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ComboBox Grid.Column="0"
                                              ItemsSource="{Binding AvailableGateways}"
                                              SelectedValue="{Binding SelectedGatewayId}"
                                              SelectedValuePath="Id"
                                              DisplayMemberPath="DisplayText"
                                              IsEnabled="{Binding CanSelectGateway}"
                                              Style="{StaticResource FormComboBoxStyle}"/>
                                    <Button Grid.Column="1"
                                            Margin="8,0,0,0"
                                            Padding="12,6"
                                            x:Name="DlgSrv_TestConnectionBtn"
                                            Command="{Binding TestGatewayCommand}"
                                            Style="{DynamicResource SecondaryButtonStyle}"/>
                                </Grid>
                                <TextBlock Text="{Binding GatewayExplanation}" Style="{StaticResource HintTextStyle}"/>
                                <Border Margin="0,12,0,0">
                                    <Border.Style>
                                        <Style TargetType="Border" BasedOn="{StaticResource InsetCardStyle}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding UsesGateway}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel>
                                        <TextBlock Text="{Binding SelectedGatewayTitle}"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding SelectedGatewayEndpoint}" Style="{StaticResource HintTextStyle}" Margin="0,4,0,0"/>
                                        <TextBlock Text="{Binding GatewayRouteText}" Style="{StaticResource HintTextStyle}" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                                <TextBlock Text="{Binding GatewayTestMessage}"
                                           Foreground="{DynamicResource InfoBrush}"
                                           FontSize="12"
                                           Margin="0,10,0,0"
                                           TextWrapping="Wrap"
                                           Visibility="{Binding GatewayTestMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_ConnectionPathTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock Text="{Binding ConnectionPathHeadline}" Style="{StaticResource SectionDescriptionStyle}"/>
                                <StackPanel Margin="0,16,0,0"
                                            Orientation="Horizontal"
                                            HorizontalAlignment="Center"
                                            Style="{StaticResource DirectSectionStyle}">
                                    <Border Style="{StaticResource DiagramNodeStyle}">
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_DirectYourPc" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding ClientNodeCaption}" Style="{StaticResource HintTextStyle}"/>
                                        </StackPanel>
                                    </Border>
                                    <StackPanel Margin="12,16,12,0" Width="78">
                                        <TextBlock Text="->" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="18" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding SessionKindLabel}" Style="{StaticResource ArrowCaptionStyle}" Margin="0,6,0,0"/>
                                    </StackPanel>
                                    <Border Style="{StaticResource DiagramNodeStyle}">
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_DirectDestination" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding DestinationNodeCaption}" Style="{StaticResource HintTextStyle}"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                                <StackPanel Margin="0,16,0,0"
                                            Orientation="Horizontal"
                                            HorizontalAlignment="Center"
                                            Style="{StaticResource GatewaySectionStyle}">
                                    <Border Style="{StaticResource DiagramNodeStyle}">
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_GatewayYourPc" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding ClientNodeCaption}" Style="{StaticResource HintTextStyle}"/>
                                        </StackPanel>
                                    </Border>
                                    <StackPanel Margin="12,16,12,0" Width="78">
                                        <TextBlock Text="->" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="18" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding ClientToGatewayLabel}" Style="{StaticResource ArrowCaptionStyle}" Margin="0,6,0,0"/>
                                    </StackPanel>
                                    <Border Style="{StaticResource DiagramNodeStyle}">
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_GatewayNode" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding SelectedGatewayTitle}" Style="{StaticResource HintTextStyle}"/>
                                            <TextBlock Text="{Binding SelectedGatewayEndpoint}" Style="{StaticResource HintTextStyle}" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <StackPanel Margin="12,16,12,0" Width="78">
                                        <TextBlock Text="->" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="18" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding GatewayToServerLabel}" Style="{StaticResource ArrowCaptionStyle}" Margin="0,6,0,0"/>
                                    </StackPanel>
                                    <Border Style="{StaticResource DiagramNodeStyle}">
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_GatewayDestination" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding DestinationNodeCaption}" Style="{StaticResource HintTextStyle}"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem x:Name="DlgSrv_TabTunneling">
                <TabItem.Style>
                    <Style TargetType="TabItem" BasedOn="{StaticResource ThemedTabItemStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ConnectionType}" Value="FTP">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ConnectionType}" Value="VNC">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ConnectionType}" Value="Telnet">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TabItem.Style>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <Border Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_TunnelingTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_TunnelingDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <TextBlock x:Name="DlgSrv_TunnelingHint"
                                           Style="{StaticResource HintTextStyle}" Margin="0,10,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding UsesGateway}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_DirectConnTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_DirectConnDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                            </StackPanel>
                        </Border>

                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding UsesGateway}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_LocalTunnelPortTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_LocalTunnelPortDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <CheckBox x:Name="DlgSrv_AutoTunnelPortCb"
                                          IsChecked="{Binding UseAutomaticTunnelPort}"
                                          Style="{StaticResource FormCheckBoxStyle}"
                                          Margin="0,14,0,0"/>
                                <TextBlock Text="{Binding LocalTunnelPortDisplay}" Style="{StaticResource HintTextStyle}" Margin="0,8,0,0"/>
                                <Grid Margin="0,12,0,0" Style="{StaticResource ManualTunnelPortStyle}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="DlgSrv_ManualPortLabel" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,6,0,0"/>
                                    <TextBox Grid.Column="1"
                                             Width="140"
                                             HorizontalAlignment="Left"
                                             Text="{Binding LocalPort, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource FormTextBoxStyle}"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding UsesGateway}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_TunnelMappingTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock Text="{Binding TunnelSummary}" Style="{StaticResource SectionDescriptionStyle}"/>
                                <Grid Margin="0,14,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock x:Name="DlgSrv_ChainLabel" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding GatewayRouteText}" Style="{StaticResource HintTextStyle}" Margin="0,0,0,0"/>
                                    <TextBlock x:Name="DlgSrv_LocalEndpointLabel" Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,12,0,0"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ClientNodeCaption}" Style="{StaticResource HintTextStyle}" Margin="0,12,0,0"/>
                                    <TextBlock x:Name="DlgSrv_RemoteDestLabel" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,12,0,0"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DestinationNodeCaption}" Style="{StaticResource HintTextStyle}" Margin="0,12,0,0"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem x:Name="DlgSrv_TabAuthentication">
                <TabItem.Style>
                    <Style TargetType="TabItem" BasedOn="{StaticResource ThemedTabItemStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TabItem.Style>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="RDP">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_RdpCredentialsTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_RdpCredentialsDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <TextBlock x:Name="DlgSrv_RdpUsernameLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                <TextBox Text="{Binding RdpUsername, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                                <TextBlock x:Name="DlgSrv_RdpPasswordLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                                <PasswordBox x:Name="RdpPasswordBox" Style="{StaticResource FormPasswordBoxStyle}"/>
                            </StackPanel>
                        </Border>

                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="SSH">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="SFTP">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_SshCredentialsTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_SshCredentialsDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <TextBlock x:Name="DlgSrv_SshUsernameLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                <TextBox Text="{Binding SshUsername, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                                <TextBlock x:Name="DlgSrv_SshKeyLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                                <DockPanel>
                                    <Button x:Name="DlgSrv_BrowseBtn"
                                            DockPanel.Dock="Right"
                                            Command="{Binding BrowseSshKeyCommand}"
                                            Style="{DynamicResource SecondaryButtonStyle}"
                                            Margin="8,0,0,0"
                                            Click="OnBrowseSshKeyClick"/>
                                    <TextBox Text="{Binding SshKeyPath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                                </DockPanel>
                                <TextBlock x:Name="DlgSrv_PassphraseLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                                <PasswordBox x:Name="SshPasswordBox" Style="{StaticResource FormPasswordBoxStyle}"/>
                                <TextBlock x:Name="DlgSrv_SshAuthHint"
                                           Style="{StaticResource HintTextStyle}" Margin="0,8,0,0"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <!-- FTP credentials (visible only for FTP type) -->
                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="FTP">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_FtpCredentialsTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_FtpCredentialsDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <TextBlock x:Name="DlgSrv_FtpUsernameLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                <TextBox Text="{Binding FtpUsername, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                                <TextBlock x:Name="DlgSrv_FtpPasswordLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                                <PasswordBox x:Name="FtpPasswordBox" Style="{StaticResource FormPasswordBoxStyle}"/>
                            </StackPanel>
                        </Border>

                        <!-- VNC credentials (visible only for VNC type) -->
                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="VNC">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_VncCredentialsTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_VncCredentialsDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <TextBlock x:Name="DlgSrv_VncPasswordLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                <PasswordBox x:Name="VncPasswordBox" Style="{StaticResource FormPasswordBoxStyle}"/>
                            </StackPanel>
                        </Border>

                        <!-- Telnet credentials (visible only for Telnet type) -->
                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="Telnet">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_TelnetCredentialsTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_TelnetCredentialsDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <TextBlock x:Name="DlgSrv_TelnetUsernameLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                <TextBox Text="{Binding TelnetUsername, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                                <TextBlock x:Name="DlgSrv_TelnetPasswordLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                                <PasswordBox x:Name="TelnetPasswordBox" Style="{StaticResource FormPasswordBoxStyle}"/>
                            </StackPanel>
                        </Border>

                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding UsesGateway}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_GatewayAuthTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_GatewayAuthDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <TextBlock Text="{Binding SelectedGatewayTitle}" Style="{StaticResource HintTextStyle}" Margin="0,10,0,0"/>
                                <TextBlock Text="{Binding SelectedGatewayEndpoint}" Style="{StaticResource HintTextStyle}" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem x:Name="DlgSrv_TabOptions" Style="{DynamicResource ThemedTabItemStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="RDP">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_RdpOptionsTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_RdpOptionsDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <Grid Margin="0,16,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock x:Name="DlgSrv_SessionModeLabel" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock x:Name="DlgSrv_AspectRatioLabel" Grid.Row="0" Grid.Column="1" Style="{StaticResource LabelStyle}" Margin="12,0,0,4"/>
                                    <ComboBox Grid.Row="1" Grid.Column="0"
                                              SelectedValue="{Binding RdpMode}"
                                              SelectedValuePath="Content"
                                              Style="{StaticResource FormComboBoxStyle}">
                                        <ComboBoxItem Content="Embedded"/>
                                        <ComboBoxItem Content="External"/>
                                    </ComboBox>
                                    <ComboBox Grid.Row="1" Grid.Column="1"
                                              Margin="12,0,0,0"
                                              SelectedValue="{Binding RdpAspectRatio}"
                                              SelectedValuePath="Content"
                                              Style="{StaticResource FormComboBoxStyle}">
                                        <ComboBoxItem Content="Stretch"/>
                                        <ComboBoxItem Content="Preserve"/>
                                    </ComboBox>
                                    <TextBlock x:Name="DlgSrv_AudioModeLabel" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                    <TextBlock x:Name="DlgSrv_ColorDepthLabel" Grid.Row="2" Grid.Column="1" Style="{StaticResource LabelStyle}" Margin="12,14,0,4"/>
                                    <ComboBox Grid.Row="3" Grid.Column="0"
                                              SelectedValue="{Binding RdpAudioMode}"
                                              SelectedValuePath="Tag"
                                              Style="{StaticResource FormComboBoxStyle}">
                                        <ComboBoxItem x:Name="DlgSrv_AudioDisabled" Tag="0"/>
                                        <ComboBoxItem x:Name="DlgSrv_AudioLocal" Tag="1"/>
                                        <ComboBoxItem x:Name="DlgSrv_AudioRemote" Tag="2"/>
                                    </ComboBox>
                                    <ComboBox Grid.Row="3" Grid.Column="1"
                                              Margin="12,0,0,0"
                                              SelectedValue="{Binding RdpColorDepth}"
                                              SelectedValuePath="Tag"
                                              Style="{StaticResource FormComboBoxStyle}">
                                        <ComboBoxItem x:Name="DlgSrv_Color16" Tag="16"/>
                                        <ComboBoxItem x:Name="DlgSrv_Color24" Tag="24"/>
                                        <ComboBoxItem x:Name="DlgSrv_Color32" Tag="32"/>
                                    </ComboBox>
                                </Grid>
                                <StackPanel Margin="0,14,0,0">
                                    <CheckBox x:Name="DlgSrv_NlaCb" IsChecked="{Binding RdpNla}" Style="{StaticResource FormCheckBoxStyle}"/>
                                    <CheckBox x:Name="DlgSrv_MultiMonitorCb" IsChecked="{Binding RdpMultiMonitor}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                                    <CheckBox x:Name="DlgSrv_DynamicResCb" IsChecked="{Binding RdpDynamicResolution}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                                    <CheckBox x:Name="DlgSrv_AudioCaptureCb" IsChecked="{Binding RdpAudioCapture}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Expander x:Name="DlgSrv_DeviceRedirExpander" IsExpanded="True">
                            <Expander.Style>
                                <Style TargetType="Expander" BasedOn="{StaticResource ThemedExpanderStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="RDP">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Expander.Style>
                            <StackPanel Margin="0,12,0,0">
                                <CheckBox x:Name="DlgSrv_RedirClipboardCb" IsChecked="{Binding RedirectClipboard}" Style="{StaticResource FormCheckBoxStyle}"/>
                                <CheckBox x:Name="DlgSrv_RedirDrivesCb" IsChecked="{Binding RedirectDrives}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                                <CheckBox x:Name="DlgSrv_RedirPrintersCb" IsChecked="{Binding RedirectPrinters}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                                <CheckBox x:Name="DlgSrv_RedirComPortsCb" IsChecked="{Binding RdpRedirectComPorts}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                                <CheckBox x:Name="DlgSrv_RedirSmartCardsCb" IsChecked="{Binding RdpRedirectSmartCards}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                                <CheckBox x:Name="DlgSrv_RedirWebcamCb" IsChecked="{Binding RdpRedirectWebcam}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                                <CheckBox x:Name="DlgSrv_RedirUsbCb" IsChecked="{Binding RdpRedirectUsb}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Expander>

                        <Expander x:Name="DlgSrv_AdvancedRdpExpander" IsExpanded="False">
                            <Expander.Style>
                                <Style TargetType="Expander" BasedOn="{StaticResource ThemedExpanderStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="RDP">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Expander.Style>
                            <StackPanel Margin="0,12,0,0">
                                <CheckBox x:Name="DlgSrv_GlobalDefaultsCb" IsChecked="{Binding RdpUseGlobalDefaults}" Style="{StaticResource FormCheckBoxStyle}"/>
                                <CheckBox x:Name="DlgSrv_AntiIdleCb" IsChecked="{Binding RdpAntiIdle}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                                <CheckBox x:Name="DlgSrv_BitmapCacheCb" IsChecked="{Binding RdpBitmapCaching}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                                <CheckBox x:Name="DlgSrv_RdpCompressionCb" IsChecked="{Binding RdpCompression}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                                <CheckBox x:Name="DlgSrv_AutoReconnectCb" IsChecked="{Binding RdpAutoReconnect}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Expander>

                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="SSH">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="SFTP">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_SshOptionsTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_SshOptionsDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <Grid Margin="0,16,0,0">
                                    <Grid.Style>
                                        <Style TargetType="Grid">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ConnectionType}" Value="SSH">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="DlgSrv_SshModeLabel" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,6,0,0"/>
                                    <ComboBox Grid.Column="1"
                                              SelectedValue="{Binding SshMode}"
                                              SelectedValuePath="Content"
                                              Style="{StaticResource FormComboBoxStyle}">
                                        <ComboBoxItem Content="Embedded"/>
                                        <ComboBoxItem Content="External"/>
                                    </ComboBox>
                                </Grid>
                                <StackPanel Margin="0,14,0,0">
                                    <CheckBox x:Name="DlgSrv_SshCompressionCb" IsChecked="{Binding SshCompression}" Style="{StaticResource FormCheckBoxStyle}"/>
                                    <CheckBox x:Name="DlgSrv_SshAgentFwdCb" IsChecked="{Binding SshAgentForwarding}" Style="{StaticResource FormCheckBoxStyle}" Margin="0,6,0,0"/>
                                    <CheckBox x:Name="DlgSrv_SshX11Cb"
                                              IsChecked="{Binding SshX11Forwarding}"
                                              Margin="0,6,0,0">
                                        <CheckBox.Style>
                                            <Style TargetType="CheckBox" BasedOn="{StaticResource FormCheckBoxStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="SSH">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </CheckBox.Style>
                                    </CheckBox>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Local Shell settings (visible only for Local type) -->
                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_LocalShellTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_LocalShellDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <Grid Margin="0,16,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock x:Name="DlgSrv_ExecutableLabel" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                                    <ComboBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                              IsEditable="True"
                                              Text="{Binding LocalShellExecutable, UpdateSourceTrigger=PropertyChanged}"
                                              Style="{StaticResource FormComboBoxStyle}">
                                        <ComboBoxItem Content="powershell.exe"/>
                                        <ComboBoxItem Content="pwsh.exe"/>
                                        <ComboBoxItem Content="cmd.exe"/>
                                        <ComboBoxItem Content="bash"/>
                                        <ComboBoxItem Content="wsl.exe"/>
                                    </ComboBox>
                                    <TextBlock x:Name="DlgSrv_ArgumentsLabel" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                    <TextBox Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                                             Text="{Binding LocalShellArguments, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource FormTextBoxStyle}"/>
                                    <TextBlock x:Name="DlgSrv_WorkingDirLabel" Grid.Row="4" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                    <TextBox Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                                             Text="{Binding LocalShellWorkingDirectory, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource FormTextBoxStyle}"/>
                                </Grid>
                                <CheckBox x:Name="DlgSrv_RunAsAdminCb"
                                          IsChecked="{Binding LocalShellElevated}"
                                          Foreground="{DynamicResource TextPrimaryBrush}"
                                          Margin="0,12,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Citrix settings (visible only for Citrix type) -->
                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_CitrixTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_CitrixDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <Grid Margin="0,16,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock x:Name="DlgSrv_StoreFrontLabel" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                             Text="{Binding CitrixStoreFrontUrl, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource FormTextBoxStyle}"/>
                                    <TextBlock x:Name="DlgSrv_AppNameLabel" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                    <TextBox Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                                             Text="{Binding CitrixAppName, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource FormTextBoxStyle}"/>
                                    <TextBlock x:Name="DlgSrv_IcaFileLabel" Grid.Row="4" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                    <TextBox Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                                             Text="{Binding CitrixIcaFilePath, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource FormTextBoxStyle}"/>
                                </Grid>
                                <TextBlock x:Name="DlgSrv_CitrixHint"
                                           Style="{StaticResource HintTextStyle}"/>
                                <CheckBox x:Name="DlgSrv_SeamlessCb"
                                          IsChecked="{Binding CitrixSeamlessMode}"
                                          Foreground="{DynamicResource TextPrimaryBrush}"
                                          Margin="0,12,0,0"/>
                                <CheckBox x:Name="DlgSrv_SsoCb"
                                          IsChecked="{Binding CitrixUseSso}"
                                          Foreground="{DynamicResource TextPrimaryBrush}"
                                          Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- FTP options (visible only for FTP type) -->
                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="FTP">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_FtpOptionsTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_FtpOptionsDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <CheckBox x:Name="DlgSrv_FtpPassiveCb"
                                          IsChecked="{Binding FtpPassiveMode}"
                                          Foreground="{DynamicResource TextPrimaryBrush}"
                                          Margin="0,14,0,0"/>
                                <CheckBox x:Name="DlgSrv_FtpSslCb"
                                          IsChecked="{Binding FtpUseSsl}"
                                          Foreground="{DynamicResource TextPrimaryBrush}"
                                          Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- VNC options (visible only for VNC type) -->
                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="VNC">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_VncOptionsTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_VncOptionsDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <CheckBox x:Name="DlgSrv_VncViewOnlyCb"
                                          IsChecked="{Binding VncViewOnly}"
                                          Foreground="{DynamicResource TextPrimaryBrush}"
                                          Margin="0,14,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Telnet options (visible only for Telnet type) -->
                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SectionCardStyle}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="Telnet">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_TelnetOptionsTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_TelnetOptionsDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <TextBlock x:Name="DlgSrv_TelnetSecurityWarning"
                                           Foreground="{DynamicResource WarningBrush}"
                                           TextWrapping="Wrap"
                                           Margin="0,14,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem x:Name="DlgSrv_TabInfo" Style="{DynamicResource ThemedTabItemStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <Border Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_OrgTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_OrgDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <Grid Margin="0,16,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock x:Name="DlgSrv_FolderLabel" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Grid.Row="1" Grid.Column="0"
                                             Text="{Binding Group, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource FormTextBoxStyle}"/>
                                    <TextBlock x:Name="DlgSrv_EnvironmentLabel" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                    <TextBlock x:Name="DlgSrv_FavoriteLabel" Grid.Row="2" Grid.Column="1" Style="{StaticResource LabelStyle}" Margin="12,14,0,4"/>
                                    <ComboBox Grid.Row="3" Grid.Column="0"
                                              SelectedValue="{Binding Environment}"
                                              SelectedValuePath="Content"
                                              Style="{StaticResource FormComboBoxStyle}">
                                        <ComboBoxItem Content="None"/>
                                        <ComboBoxItem Content="Production"/>
                                        <ComboBoxItem Content="Staging"/>
                                        <ComboBoxItem Content="Lab"/>
                                        <ComboBoxItem Content="Personal"/>
                                    </ComboBox>
                                    <CheckBox Grid.Row="3" Grid.Column="1"
                                              Margin="12,2,0,0"
                                              x:Name="DlgSrv_MarkFavoriteCb"
                                              IsChecked="{Binding IsFavorite}"
                                              Style="{StaticResource FormCheckBoxStyle}"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="DlgSrv_MetadataTitle" Style="{StaticResource SectionTitleStyle}"/>
                                <TextBlock x:Name="DlgSrv_MetadataDesc"
                                           Style="{StaticResource SectionDescriptionStyle}"/>
                                <TextBlock x:Name="DlgSrv_TagsLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                <TextBox Text="{Binding Tags, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                                <TextBlock x:Name="DlgSrv_MacAddressLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                                <TextBox Text="{Binding MacAddress, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <TextBlock Grid.Row="1"
                   Text="{Binding ValidationError}"
                   Foreground="{DynamicResource ErrorBrush}"
                   FontSize="12"
                   Margin="0,8,0,0"
                   TextWrapping="Wrap"
                   Visibility="{Binding ValidationError, Converter={StaticResource NullToVisibilityConverter}}"/>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="DlgSrv_CancelBtn"
                    IsCancel="True"
                    Width="90"
                    Margin="0,0,8,0"
                    Style="{DynamicResource SecondaryButtonStyle}"/>
            <Button x:Name="DlgSrv_SaveBtn"
                    IsDefault="True"
                    Width="90"
                    Click="OnSaveClick"
                    Style="{DynamicResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
