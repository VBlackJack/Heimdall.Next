<Window x:Class="Heimdall.App.Views.Dialogs.ServerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:Heimdall.App.Converters"
        xmlns:vm="clr-namespace:Heimdall.App.ViewModels.Dialogs"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        d:DataContext="{d:DesignInstance Type=vm:ServerDialogViewModel}"
        mc:Ignorable="d"
        Title="{Binding DialogTitle}"
        Width="650"
        SizeToContent="Height"
        MaxHeight="750"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        ResizeMode="CanResizeWithGrip"
        MinWidth="550"
        MinHeight="350"
        ShowInTaskbar="False"
        Closing="OnWindowClosing">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/DialogCommonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

            <!-- ServerDialog label extends DialogLabelStyle with SemiBold weight -->
            <Style x:Key="LabelStyle" TargetType="TextBlock" BasedOn="{StaticResource DialogLabelStyle}">
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>

            <Style x:Key="InsetCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Padding" Value="12"/>
            </Style>

            <Style x:Key="DiagramNodeStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Padding" Value="12"/>
                <Setter Property="Width" Value="165"/>
                <Setter Property="MinHeight" Value="86"/>
            </Style>

            <Style x:Key="ArrowCaptionStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                <Setter Property="FontSize" Value="{StaticResource FontSizeCaption}"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="TextAlignment" Value="Center"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
            </Style>

            <Style x:Key="RdpOnlySectionStyle" TargetType="FrameworkElement">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding ConnectionType}" Value="RDP">
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="SshOnlySectionStyle" TargetType="FrameworkElement">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding ConnectionType}" Value="SSH">
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="SshFamilySectionStyle" TargetType="FrameworkElement">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding ConnectionType}" Value="SSH">
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding ConnectionType}" Value="SFTP">
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <!-- Gateway/routing sections: hidden for Local shell, Citrix, VNC, FTP -->
            <Style x:Key="NetworkSectionStyle" TargetType="FrameworkElement">
                <Setter Property="Visibility" Value="Visible"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding ConnectionType}" Value="VNC">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding ConnectionType}" Value="FTP">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="GatewaySectionStyle" TargetType="FrameworkElement">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding UsesGateway}" Value="True">
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="DirectSectionStyle" TargetType="FrameworkElement">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding UsesGateway}" Value="False">
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="ManualTunnelPortStyle" TargetType="FrameworkElement">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding UsesGateway}" Value="True"/>
                            <Condition Binding="{Binding UseAutomaticTunnelPort}" Value="False"/>
                        </MultiDataTrigger.Conditions>
                        <Setter Property="Visibility" Value="Visible"/>
                    </MultiDataTrigger>
                </Style.Triggers>
            </Style>

            <!-- Protocol card button style (Step 1 selector) -->
            <Style x:Key="ProtocolCardButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Padding" Value="12"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="MinHeight" Value="100"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="CardBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{StaticResource CornerRadiusLg}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="CardBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    <Setter TargetName="CardBorder" Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="CardBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="Opacity" Value="0.85"/>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="CardBorder" Property="BorderBrush" Value="{DynamicResource FocusIndicatorBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Advanced settings toggle button style -->
            <Style x:Key="AdvancedToggleStyle" TargetType="ToggleButton">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                <Setter Property="FontSize" Value="{StaticResource FontSizeBody}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="FocusBorder"
                                    BorderBrush="Transparent"
                                    BorderThickness="0,0,0,2"
                                    Padding="2,0,2,2"
                                    Background="Transparent">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="Arrow" Text="&#x25B8;"
                                               Margin="0,0,6,0"
                                               Foreground="{TemplateBinding Foreground}"
                                               FontSize="{TemplateBinding FontSize}"/>
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Arrow" Property="Text" Value="&#x25BE;"/>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="FocusBorder" Property="BorderBrush" Value="{DynamicResource FocusIndicatorBrush}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.8"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="{StaticResource SpacingLg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="{StaticResource SpacingSm}">

                <!-- ================================================================ -->
                <!-- STEP 1: PROTOCOL SELECTOR (add mode only, before selection)      -->
                <!-- ================================================================ -->

                <StackPanel x:Name="ProtocolSelectorPanel"
                            Visibility="{Binding ShowProtocolSelector, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock x:Name="DlgSrv_ProtocolSelectorTitle"
                               Style="{StaticResource DialogSectionTitleStyle}"
                               FontSize="{StaticResource FontSizeTitle}"
                               Margin="0,0,0,4"/>
                    <TextBlock x:Name="DlgSrv_ProtocolSelectorDesc"
                               Style="{StaticResource DialogSectionDescriptionStyle}"
                               Margin="0,0,0,16"/>

                    <ItemsControl x:Name="ProtocolCards">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="4"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <!-- RDP -->
                        <Button x:Name="ProtocolCard_Rdp" Command="{Binding SelectProtocolCommand}" CommandParameter="RDP"
                                Style="{StaticResource ProtocolCardButtonStyle}">
                            <StackPanel HorizontalAlignment="Center">
                                <Path Data="{StaticResource Geo.Protocol.Rdp}"
                                      Fill="{DynamicResource ProtocolRdpBrush}"
                                      Stretch="Uniform" Width="28" Height="28"
                                      HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock x:Name="DlgSrv_ProtoRdpName" HorizontalAlignment="Center"
                                           FontWeight="SemiBold" FontSize="{StaticResource FontSizeBody}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock x:Name="DlgSrv_ProtoRdpDesc" HorizontalAlignment="Center"
                                           TextAlignment="Center" TextWrapping="Wrap"
                                           FontSize="{StaticResource FontSizeCaption}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Button>

                        <!-- SSH -->
                        <Button x:Name="ProtocolCard_Ssh" Command="{Binding SelectProtocolCommand}" CommandParameter="SSH"
                                Style="{StaticResource ProtocolCardButtonStyle}">
                            <StackPanel HorizontalAlignment="Center">
                                <Path Data="{StaticResource Geo.Protocol.Ssh}"
                                      Fill="{DynamicResource ProtocolSshBrush}"
                                      Stretch="Uniform" Width="28" Height="28"
                                      HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock x:Name="DlgSrv_ProtoSshName" HorizontalAlignment="Center"
                                           FontWeight="SemiBold" FontSize="{StaticResource FontSizeBody}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock x:Name="DlgSrv_ProtoSshDesc" HorizontalAlignment="Center"
                                           TextAlignment="Center" TextWrapping="Wrap"
                                           FontSize="{StaticResource FontSizeCaption}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Button>

                        <!-- SFTP -->
                        <Button x:Name="ProtocolCard_Sftp" Command="{Binding SelectProtocolCommand}" CommandParameter="SFTP"
                                Style="{StaticResource ProtocolCardButtonStyle}">
                            <StackPanel HorizontalAlignment="Center">
                                <Path Data="{StaticResource Geo.Protocol.Sftp}"
                                      Fill="{DynamicResource ProtocolSftpBrush}"
                                      Stretch="Uniform" Width="28" Height="28"
                                      HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock x:Name="DlgSrv_ProtoSftpName" HorizontalAlignment="Center"
                                           FontWeight="SemiBold" FontSize="{StaticResource FontSizeBody}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock x:Name="DlgSrv_ProtoSftpDesc" HorizontalAlignment="Center"
                                           TextAlignment="Center" TextWrapping="Wrap"
                                           FontSize="{StaticResource FontSizeCaption}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Button>

                        <!-- VNC -->
                        <Button x:Name="ProtocolCard_Vnc" Command="{Binding SelectProtocolCommand}" CommandParameter="VNC"
                                Style="{StaticResource ProtocolCardButtonStyle}">
                            <StackPanel HorizontalAlignment="Center">
                                <Path Data="{StaticResource Geo.Protocol.Vnc}"
                                      Fill="{DynamicResource ProtocolVncBrush}"
                                      Stretch="Uniform" Width="28" Height="28"
                                      HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock x:Name="DlgSrv_ProtoVncName" HorizontalAlignment="Center"
                                           FontWeight="SemiBold" FontSize="{StaticResource FontSizeBody}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock x:Name="DlgSrv_ProtoVncDesc" HorizontalAlignment="Center"
                                           TextAlignment="Center" TextWrapping="Wrap"
                                           FontSize="{StaticResource FontSizeCaption}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Button>

                        <!-- Telnet -->
                        <Button x:Name="ProtocolCard_Telnet" Command="{Binding SelectProtocolCommand}" CommandParameter="Telnet"
                                Style="{StaticResource ProtocolCardButtonStyle}">
                            <StackPanel HorizontalAlignment="Center">
                                <Path Data="{StaticResource Geo.Protocol.Telnet}"
                                      Fill="{DynamicResource ProtocolTelnetBrush}"
                                      Stretch="Uniform" Width="28" Height="28"
                                      HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock x:Name="DlgSrv_ProtoTelnetName" HorizontalAlignment="Center"
                                           FontWeight="SemiBold" FontSize="{StaticResource FontSizeBody}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock x:Name="DlgSrv_ProtoTelnetDesc" HorizontalAlignment="Center"
                                           TextAlignment="Center" TextWrapping="Wrap"
                                           FontSize="{StaticResource FontSizeCaption}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Button>

                        <!-- FTP -->
                        <Button x:Name="ProtocolCard_Ftp" Command="{Binding SelectProtocolCommand}" CommandParameter="FTP"
                                Style="{StaticResource ProtocolCardButtonStyle}">
                            <StackPanel HorizontalAlignment="Center">
                                <Path Data="{StaticResource Geo.Protocol.Ftp}"
                                      Fill="{DynamicResource ProtocolFtpBrush}"
                                      Stretch="Uniform" Width="28" Height="28"
                                      HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock x:Name="DlgSrv_ProtoFtpName" HorizontalAlignment="Center"
                                           FontWeight="SemiBold" FontSize="{StaticResource FontSizeBody}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock x:Name="DlgSrv_ProtoFtpDesc" HorizontalAlignment="Center"
                                           TextAlignment="Center" TextWrapping="Wrap"
                                           FontSize="{StaticResource FontSizeCaption}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Button>

                        <!-- Citrix -->
                        <Button x:Name="ProtocolCard_Citrix" Command="{Binding SelectProtocolCommand}" CommandParameter="Citrix"
                                Style="{StaticResource ProtocolCardButtonStyle}">
                            <StackPanel HorizontalAlignment="Center">
                                <Path Data="{StaticResource Geo.Protocol.Citrix}"
                                      Fill="{DynamicResource ProtocolCitrixBrush}"
                                      Stretch="Uniform" Width="28" Height="28"
                                      HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock x:Name="DlgSrv_ProtoCitrixName" HorizontalAlignment="Center"
                                           FontWeight="SemiBold" FontSize="{StaticResource FontSizeBody}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock x:Name="DlgSrv_ProtoCitrixDesc" HorizontalAlignment="Center"
                                           TextAlignment="Center" TextWrapping="Wrap"
                                           FontSize="{StaticResource FontSizeCaption}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Button>

                        <!-- Local Shell -->
                        <Button x:Name="ProtocolCard_Local" Command="{Binding SelectProtocolCommand}" CommandParameter="Local"
                                Style="{StaticResource ProtocolCardButtonStyle}">
                            <StackPanel HorizontalAlignment="Center">
                                <Path Data="{StaticResource Geo.Protocol.LocalShell}"
                                      Fill="{DynamicResource ProtocolLocalBrush}"
                                      Stretch="Uniform" Width="28" Height="28"
                                      HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock x:Name="DlgSrv_ProtoLocalName" HorizontalAlignment="Center"
                                           FontWeight="SemiBold" FontSize="{StaticResource FontSizeBody}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock x:Name="DlgSrv_ProtoLocalDesc" HorizontalAlignment="Center"
                                           TextAlignment="Center" TextWrapping="Wrap"
                                           FontSize="{StaticResource FontSizeCaption}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Button>
                    </ItemsControl>
                </StackPanel>

                <!-- ================================================================ -->
                <!-- STEP 2: FORM FIELDS (visible after protocol selection / edit)     -->
                <!-- ================================================================ -->

                <StackPanel x:Name="FormFieldsPanel"
                            Visibility="{Binding ShowFormFields, Converter={StaticResource BoolToVisibilityConverter}}">

                <!-- Protocol badge (edit mode: read-only indicator) -->
                <Border Margin="0,0,0,12">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="DlgSrv_ProtocolBadgeLabel"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   FontSize="{StaticResource FontSizeCaption}"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Border CornerRadius="{StaticResource CornerRadiusLg}" Padding="10,4"
                                Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1">
                            <TextBlock Text="{Binding ConnectionType}"
                                       FontWeight="SemiBold"
                                       FontSize="{StaticResource FontSizeBody}"
                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Back button (add mode only, after protocol selection) -->
                <Button x:Name="DlgSrv_BackBtn" Margin="0,0,0,12"
                        Command="{Binding BackToProtocolSelectorCommand}"
                        MinWidth="80" HorizontalAlignment="Left">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Connection basics: Name, Type, Host, Port -->
                <Border Style="{StaticResource DialogSectionCardStyle}">
                    <StackPanel>
                        <TextBlock x:Name="DlgSrv_ConnectionBasicsTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                        <TextBlock x:Name="DlgSrv_ConnectionBasicsDesc"
                                   Style="{StaticResource DialogSectionDescriptionStyle}"/>
                        <Grid Margin="0,16,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock x:Name="DlgSrv_DisplayNameLabel" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="DlgSrv_DisplayNameBox" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                     TabIndex="0"
                                     Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource DialogFormTextBoxStyle}"/>
                            <!-- Inline validation: Display Name -->
                            <TextBlock Grid.Row="2" Grid.Column="0"
                                       Text="{Binding DisplayNameError}"
                                       Style="{StaticResource FieldValidationErrorStyle}"
                                       Visibility="{Binding DisplayNameError, Converter={StaticResource NullToVisibilityConverter}}"/>
                            <!-- Server/Port row: hidden for Local shell and Citrix -->
                            <TextBlock x:Name="DlgSrv_ServerLabel" Grid.Row="3" Grid.Column="0" Margin="0,14,0,4">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource LabelStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding EndpointPortLabel}" Margin="12,14,0,4">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource LabelStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBox x:Name="DlgSrv_RemoteServerBox" Grid.Row="4" Grid.Column="0"
                                     TabIndex="2"
                                     Text="{Binding RemoteServer, UpdateSourceTrigger=PropertyChanged}">
                                <TextBox.Style>
                                    <Style TargetType="TextBox" BasedOn="{StaticResource DialogFormTextBoxStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                            <TextBox x:Name="DlgSrv_EndpointPortBox" Grid.Row="4" Grid.Column="1"
                                     TabIndex="3"
                                     Margin="12,0,0,0"
                                     Width="120"
                                     HorizontalAlignment="Left"
                                     Text="{Binding EndpointPort, UpdateSourceTrigger=PropertyChanged}">
                                <TextBox.Style>
                                    <Style TargetType="TextBox" BasedOn="{StaticResource DialogFormTextBoxStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                            <!-- Inline validation: Server address -->
                            <TextBlock Grid.Row="5" Grid.Column="0"
                                       Text="{Binding RemoteServerError}"
                                       Style="{StaticResource FieldValidationErrorStyle}"
                                       Visibility="{Binding RemoteServerError, Converter={StaticResource NullToVisibilityConverter}}"/>
                            <!-- Inline validation: Port -->
                            <TextBlock Grid.Row="5" Grid.Column="1"
                                       Text="{Binding EndpointPortError}"
                                       Style="{StaticResource FieldValidationErrorStyle}"
                                       Margin="12,2,0,0"
                                       Visibility="{Binding EndpointPortError, Converter={StaticResource NullToVisibilityConverter}}"/>
                        </Grid>
                        <TextBlock Text="{Binding EndpointPortHelpText}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource DialogHintTextStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Project + Gateway routing (compact) -->
                <Border Style="{StaticResource DialogSectionCardStyle}">
                    <StackPanel>
                        <TextBlock x:Name="DlgSrv_ProjectLabel" Style="{StaticResource LabelStyle}"/>
                        <ComboBox TabIndex="4"
                                  ItemsSource="{Binding AvailableProjects}"
                                  SelectedValue="{Binding SelectedProjectId}"
                                  SelectedValuePath="Id"
                                  DisplayMemberPath="Name"
                                  Style="{StaticResource DialogFormComboBoxStyle}"/>

                        <!-- Gateway section (hidden for Local, Citrix, VNC, FTP) -->
                        <StackPanel Style="{StaticResource NetworkSectionStyle}" Margin="0,14,0,0">
                            <TextBlock x:Name="DlgSrv_GatewayRoutingTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                            <TextBlock x:Name="DlgSrv_GatewayRoutingDesc"
                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                            <CheckBox x:Name="DlgSrv_DirectConnectCb"
                                      TabIndex="5"
                                      IsChecked="{Binding DirectConnection}"
                                      Style="{StaticResource DialogFormCheckBoxStyle}"
                                      Margin="0,14,0,0"/>
                            <Grid Margin="0,12,0,0">
                                <ComboBox
                                          TabIndex="6"
                                          ItemsSource="{Binding AvailableGateways}"
                                          SelectedValue="{Binding SelectedGatewayId}"
                                          SelectedValuePath="Id"
                                          DisplayMemberPath="DisplayText"
                                          IsEnabled="{Binding CanSelectGateway}"
                                          Style="{StaticResource DialogFormComboBoxStyle}"/>
                            </Grid>
                            <TextBlock Text="{Binding GatewayExplanation}" Style="{StaticResource DialogHintTextStyle}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- ================================================================ -->
                <!-- AUTHENTICATION (always visible, per-protocol)                    -->
                <!-- ================================================================ -->

                <!-- RDP credentials -->
                <Border>
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ConnectionType}" Value="RDP">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock x:Name="DlgSrv_BasicRdpCredentialsTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicRdpCredentialsDesc"
                                   Style="{StaticResource DialogSectionDescriptionStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicRdpUsernameLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                        <TextBox x:Name="DlgSrv_BasicRdpUsernameBox" TabIndex="10"
                                 Text="{Binding RdpUsername, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource DialogFormTextBoxStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicRdpPasswordLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                        <PasswordBox x:Name="RdpPasswordBox" TabIndex="11" Style="{StaticResource DialogFormPasswordBoxStyle}"/>
                    </StackPanel>
                </Border>

                <!-- SSH/SFTP credentials -->
                <Border>
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ConnectionType}" Value="SSH">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ConnectionType}" Value="SFTP">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock x:Name="DlgSrv_BasicSshCredentialsTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicSshCredentialsDesc"
                                   Style="{StaticResource DialogSectionDescriptionStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicSshUsernameLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                        <TextBox x:Name="DlgSrv_BasicSshUsernameBox" TabIndex="10"
                                 Text="{Binding SshUsername, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource DialogFormTextBoxStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicSshKeyLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                        <DockPanel>
                            <Button x:Name="DlgSrv_BasicBrowseBtn"
                                    DockPanel.Dock="Right"
                                    Command="{Binding BrowseSshKeyCommand}"
                                    Style="{DynamicResource SecondaryButtonStyle}"
                                    Margin="8,0,0,0"
                                    Click="OnBrowseSshKeyClick"/>
                            <TextBox TabIndex="11" Text="{Binding SshKeyPath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DialogFormTextBoxStyle}"/>
                        </DockPanel>
                        <TextBlock x:Name="DlgSrv_BasicPassphraseLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                        <PasswordBox x:Name="SshPasswordBox" TabIndex="12" Style="{StaticResource DialogFormPasswordBoxStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicSshAuthHint"
                                   Style="{StaticResource DialogHintTextStyle}" Margin="0,8,0,0"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- VNC credentials -->
                <Border>
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ConnectionType}" Value="VNC">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock x:Name="DlgSrv_BasicVncCredentialsTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicVncCredentialsDesc"
                                   Style="{StaticResource DialogSectionDescriptionStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicVncPasswordLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                        <PasswordBox x:Name="VncPasswordBox" TabIndex="10" Style="{StaticResource DialogFormPasswordBoxStyle}"/>
                    </StackPanel>
                </Border>

                <!-- FTP credentials -->
                <Border>
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ConnectionType}" Value="FTP">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock x:Name="DlgSrv_BasicFtpCredentialsTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicFtpCredentialsDesc"
                                   Style="{StaticResource DialogSectionDescriptionStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicFtpUsernameLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                        <TextBox TabIndex="10" Text="{Binding FtpUsername, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DialogFormTextBoxStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicFtpPasswordLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                        <PasswordBox x:Name="FtpPasswordBox" TabIndex="11" Style="{StaticResource DialogFormPasswordBoxStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Telnet credentials -->
                <Border>
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ConnectionType}" Value="Telnet">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock x:Name="DlgSrv_BasicTelnetCredentialsTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicTelnetCredentialsDesc"
                                   Style="{StaticResource DialogSectionDescriptionStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicTelnetUsernameLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                        <TextBox TabIndex="10" Text="{Binding TelnetUsername, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DialogFormTextBoxStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicTelnetPasswordLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                        <PasswordBox x:Name="TelnetPasswordBox" TabIndex="11" Style="{StaticResource DialogFormPasswordBoxStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Local Shell settings -->
                <Border>
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock x:Name="DlgSrv_BasicLocalShellTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicLocalShellDesc"
                                   Style="{StaticResource DialogSectionDescriptionStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicExecutableLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                        <ComboBox x:Name="DlgSrv_BasicShellCombo"
                                  TabIndex="10"
                                  IsEditable="True"
                                  TextSearch.TextPath="Tag"
                                  Text="{Binding LocalShellExecutable, UpdateSourceTrigger=PropertyChanged}"
                                  Style="{StaticResource DialogFormComboBoxStyle}">
                            <ComboBoxItem x:Name="DlgSrv_BasicShellPowershell" Content="" Tag="powershell.exe"/>
                            <ComboBoxItem x:Name="DlgSrv_BasicShellPwsh" Content="" Tag="pwsh.exe"/>
                            <ComboBoxItem x:Name="DlgSrv_BasicShellCmd" Content="" Tag="cmd.exe"/>
                            <ComboBoxItem x:Name="DlgSrv_BasicShellBash" Content="" Tag="bash"/>
                            <ComboBoxItem x:Name="DlgSrv_BasicShellWsl" Content="" Tag="wsl.exe"/>
                        </ComboBox>
                        <TextBlock x:Name="DlgSrv_BasicArgumentsLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                        <TextBox TabIndex="11"
                                 Text="{Binding LocalShellArguments, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource DialogFormTextBoxStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Citrix settings -->
                <Border>
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock x:Name="DlgSrv_BasicCitrixTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicCitrixDesc"
                                   Style="{StaticResource DialogSectionDescriptionStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicStoreFrontLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                        <TextBox TabIndex="10"
                                 Text="{Binding CitrixStoreFrontUrl, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource DialogFormTextBoxStyle}"/>
                        <TextBlock x:Name="DlgSrv_BasicAppNameLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                        <TextBox TabIndex="11"
                                 Text="{Binding CitrixAppName, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource DialogFormTextBoxStyle}"/>
                    </StackPanel>
                </Border>

                <!-- ================================================================ -->
                <!-- ADVANCED SETTINGS TOGGLE                                         -->
                <!-- ================================================================ -->

                <ToggleButton x:Name="DlgSrv_AdvancedToggle"
                              IsChecked="{Binding IsAdvancedMode}"
                              Style="{StaticResource AdvancedToggleStyle}"
                              Margin="0,0,0,12"
                              AutomationProperties.Name="Advanced settings"/>

                <!-- ================================================================ -->
                <!-- ADVANCED PANEL (slide-down animation)                            -->
                <!-- ================================================================ -->

                <Border ClipToBounds="True">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="LayoutTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleY="0"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Opacity" Value="0"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsAdvancedMode}" Value="True">
                                    <Setter Property="LayoutTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleY="1"/>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Opacity" Value="1"/>
                                    <DataTrigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(FrameworkElement.LayoutTransform).(ScaleTransform.ScaleY)"
                                                                 From="0" To="1" Duration="0:0:0.3">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                 From="0" To="1" Duration="0:0:0.25"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.EnterActions>
                                    <DataTrigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(FrameworkElement.LayoutTransform).(ScaleTransform.ScaleY)"
                                                                 From="1" To="0" Duration="0:0:0.25">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseIn"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                 From="1" To="0" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.ExitActions>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <TabControl x:Name="MainTabControl" Style="{DynamicResource ThemedTabControlStyle}">

                        <!-- ============================================ -->
                        <!-- Tab: Connection details (path diagram)       -->
                        <!-- ============================================ -->
                        <TabItem x:Name="DlgSrv_TabConnection" Style="{DynamicResource ThemedTabItemStyle}">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Margin="{StaticResource SpacingSm}">
                                    <!-- Gateway detail card (visible only when gateway selected) -->
                                    <Border Margin="0,0,0,12">
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding UsesGateway}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock Text="{Binding SelectedGatewayTitle}"
                                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                                       FontSize="{StaticResource FontSizeBodyLarge}"
                                                       FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding SelectedGatewayEndpoint}" Style="{StaticResource DialogHintTextStyle}" Margin="0,4,0,0"/>
                                            <TextBlock Text="{Binding GatewayRouteText}" Style="{StaticResource DialogHintTextStyle}" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Connection path diagram -->
                                    <Border Style="{StaticResource DialogSectionCardStyle}">
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_ConnectionPathTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock Text="{Binding ConnectionPathHeadline}" Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <StackPanel Margin="0,16,0,0"
                                                        Orientation="Horizontal"
                                                        HorizontalAlignment="Center"
                                                        Style="{StaticResource DirectSectionStyle}">
                                                <Border Style="{StaticResource DiagramNodeStyle}">
                                                    <StackPanel>
                                                        <TextBlock x:Name="DlgSrv_DirectYourPc" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                                        <TextBlock Text="{Binding ClientNodeCaption}" Style="{StaticResource DialogHintTextStyle}"/>
                                                    </StackPanel>
                                                </Border>
                                                <StackPanel Margin="12,16,12,0" Width="78">
                                                    <TextBlock Text="->" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="{StaticResource FontSizeTitle}" HorizontalAlignment="Center"/>
                                                    <TextBlock Text="{Binding SessionKindLabel}" Style="{StaticResource ArrowCaptionStyle}" Margin="0,6,0,0"/>
                                                </StackPanel>
                                                <Border Style="{StaticResource DiagramNodeStyle}">
                                                    <StackPanel>
                                                        <TextBlock x:Name="DlgSrv_DirectDestination" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                                        <TextBlock Text="{Binding DestinationNodeCaption}" Style="{StaticResource DialogHintTextStyle}"/>
                                                    </StackPanel>
                                                </Border>
                                            </StackPanel>
                                            <Viewbox Margin="0,16,0,0"
                                                     Stretch="Uniform"
                                                     StretchDirection="DownOnly"
                                                     Style="{StaticResource GatewaySectionStyle}">
                                                <StackPanel Orientation="Horizontal"
                                                            HorizontalAlignment="Center">
                                                    <Border Style="{StaticResource DiagramNodeStyle}">
                                                        <StackPanel>
                                                            <TextBlock x:Name="DlgSrv_GatewayYourPc" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                                            <TextBlock Text="{Binding ClientNodeCaption}" Style="{StaticResource DialogHintTextStyle}"/>
                                                        </StackPanel>
                                                    </Border>
                                                    <StackPanel Margin="12,16,12,0" Width="78">
                                                        <TextBlock Text="&#x2192;" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="{StaticResource FontSizeTitle}" HorizontalAlignment="Center"/>
                                                        <TextBlock Text="{Binding ClientToGatewayLabel}" Style="{StaticResource ArrowCaptionStyle}" Margin="0,6,0,0"/>
                                                    </StackPanel>
                                                    <Border Style="{StaticResource DiagramNodeStyle}">
                                                        <StackPanel>
                                                            <TextBlock x:Name="DlgSrv_GatewayNode" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                                            <TextBlock Text="{Binding SelectedGatewayTitle}" Style="{StaticResource DialogHintTextStyle}"/>
                                                            <TextBlock Text="{Binding SelectedGatewayEndpoint}" Style="{StaticResource DialogHintTextStyle}" Margin="0,4,0,0"/>
                                                        </StackPanel>
                                                    </Border>
                                                    <StackPanel Margin="12,16,12,0" Width="78">
                                                        <TextBlock Text="&#x2192;" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="{StaticResource FontSizeTitle}" HorizontalAlignment="Center"/>
                                                        <TextBlock Text="{Binding GatewayToServerLabel}" Style="{StaticResource ArrowCaptionStyle}" Margin="0,6,0,0"/>
                                                    </StackPanel>
                                                    <Border Style="{StaticResource DiagramNodeStyle}">
                                                        <StackPanel>
                                                            <TextBlock x:Name="DlgSrv_GatewayDestination" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                                            <TextBlock Text="{Binding DestinationNodeCaption}" Style="{StaticResource DialogHintTextStyle}"/>
                                                        </StackPanel>
                                                    </Border>
                                                </StackPanel>
                                            </Viewbox>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>

                        <!-- ============================================ -->
                        <!-- Tab: Tunneling                               -->
                        <!-- ============================================ -->
                        <TabItem x:Name="DlgSrv_TabTunneling"
                                 ToolTipService.ShowOnDisabled="True">
                            <TabItem.Header>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="DlgSrv_TabTunnelingText"/>
                                    <Border CornerRadius="8"
                                            Background="{DynamicResource ErrorTextBrush}"
                                            MinWidth="18" Height="18"
                                            Padding="5,0" Margin="6,0,0,0"
                                            VerticalAlignment="Center"
                                            Visibility="{Binding HasTunnelingTabErrors, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock Text="{Binding TunnelingTabErrorCount}"
                                                   Foreground="{DynamicResource TextOnAccentBrush}" FontSize="10"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                            </TabItem.Header>
                            <TabItem.Style>
                                <Style TargetType="TabItem" BasedOn="{StaticResource ThemedTabItemStyle}">
                                    <Setter Property="IsEnabled" Value="True"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="FTP">
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="VNC">
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="Telnet">
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TabItem.Style>
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Margin="{StaticResource SpacingSm}">
                                    <Border Style="{StaticResource DialogSectionCardStyle}">
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_TunnelingTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock x:Name="DlgSrv_TunnelingDesc"
                                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <TextBlock x:Name="DlgSrv_TunnelingHint"
                                                       Style="{StaticResource DialogHintTextStyle}" Margin="0,10,0,0"/>
                                        </StackPanel>
                                    </Border>

                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding UsesGateway}" Value="False">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_DirectConnTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock x:Name="DlgSrv_DirectConnDesc"
                                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                        </StackPanel>
                                    </Border>

                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding UsesGateway}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_LocalTunnelPortTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock x:Name="DlgSrv_LocalTunnelPortDesc"
                                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <CheckBox x:Name="DlgSrv_AutoTunnelPortCb"
                                                      TabIndex="20"
                                                      IsChecked="{Binding UseAutomaticTunnelPort}"
                                                      Style="{StaticResource DialogFormCheckBoxStyle}"
                                                      Margin="0,14,0,0"/>
                                            <TextBlock Text="{Binding LocalTunnelPortDisplay}" Style="{StaticResource DialogHintTextStyle}" Margin="0,8,0,0"/>
                                            <Grid Margin="0,12,0,0" Style="{StaticResource ManualTunnelPortStyle}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="140"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock x:Name="DlgSrv_ManualPortLabel" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,6,0,0"/>
                                                <TextBox x:Name="DlgSrv_LocalPortBox" Grid.Column="1"
                                                         TabIndex="21"
                                                         Width="140"
                                                         HorizontalAlignment="Left"
                                                         Text="{Binding LocalPort, UpdateSourceTrigger=PropertyChanged}"
                                                         Style="{StaticResource DialogFormTextBoxStyle}"/>
                                            </Grid>
                                            <!-- Inline validation: Local tunnel port -->
                                            <TextBlock Text="{Binding LocalPortError}"
                                                       Style="{StaticResource FieldValidationErrorStyle}"
                                                       Visibility="{Binding LocalPortError, Converter={StaticResource NullToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </Border>

                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding UsesGateway}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_TunnelMappingTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock Text="{Binding TunnelSummary}" Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <Grid Margin="0,14,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="150"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock x:Name="DlgSrv_ChainLabel" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding GatewayRouteText}" Style="{StaticResource DialogHintTextStyle}" Margin="0,0,0,0"/>
                                                <TextBlock x:Name="DlgSrv_LocalEndpointLabel" Grid.Row="1" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,12,0,0"/>
                                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ClientNodeCaption}" Style="{StaticResource DialogHintTextStyle}" Margin="0,12,0,0"/>
                                                <TextBlock x:Name="DlgSrv_RemoteDestLabel" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,12,0,0"/>
                                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DestinationNodeCaption}" Style="{StaticResource DialogHintTextStyle}" Margin="0,12,0,0"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>

                        <!-- ============================================ -->
                        <!-- Tab: Gateway Authentication                  -->
                        <!-- ============================================ -->
                        <TabItem x:Name="DlgSrv_TabAuthentication"
                                 ToolTipService.ShowOnDisabled="True">
                            <TabItem.Style>
                                <Style TargetType="TabItem" BasedOn="{StaticResource ThemedTabItemStyle}">
                                    <Setter Property="IsEnabled" Value="False"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding UsesGateway}" Value="True">
                                            <Setter Property="IsEnabled" Value="True"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TabItem.Style>
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Margin="{StaticResource SpacingSm}">
                                    <!-- Gateway auth info -->
                                    <Border Style="{StaticResource DialogSectionCardStyle}">
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_GatewayAuthTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock x:Name="DlgSrv_GatewayAuthDesc"
                                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <TextBlock Text="{Binding SelectedGatewayTitle}" Style="{StaticResource DialogHintTextStyle}" Margin="0,10,0,0"/>
                                            <TextBlock Text="{Binding SelectedGatewayEndpoint}" Style="{StaticResource DialogHintTextStyle}" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>

                        <!-- ============================================ -->
                        <!-- Tab: Options                                 -->
                        <!-- ============================================ -->
                        <TabItem x:Name="DlgSrv_TabOptions" Style="{DynamicResource ThemedTabItemStyle}">
                            <TabItem.Header>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="DlgSrv_TabOptionsText"/>
                                    <Border CornerRadius="8"
                                            Background="{DynamicResource ErrorTextBrush}"
                                            MinWidth="18" Height="18"
                                            Padding="5,0" Margin="6,0,0,0"
                                            VerticalAlignment="Center"
                                            Visibility="{Binding HasOptionsTabErrors, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock Text="{Binding OptionsTabErrorCount}"
                                                   Foreground="{DynamicResource TextOnAccentBrush}" FontSize="10"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                            </TabItem.Header>
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Margin="{StaticResource SpacingSm}">
                                    <!-- RDP options -->
                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="RDP">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_RdpOptionsTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock x:Name="DlgSrv_RdpOptionsDesc"
                                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <Grid Margin="0,16,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock x:Name="DlgSrv_SessionModeLabel" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                                                <TextBlock x:Name="DlgSrv_AspectRatioLabel" Grid.Row="0" Grid.Column="1" Style="{StaticResource LabelStyle}" Margin="12,0,0,4"/>
                                                <ComboBox Grid.Row="1" Grid.Column="0"
                                                          TabIndex="60"
                                                          SelectedValue="{Binding RdpMode}"
                                                          SelectedValuePath="Tag"
                                                          Style="{StaticResource DialogFormComboBoxStyle}">
                                                    <ComboBoxItem x:Name="DlgSrv_RdpModeEmbedded" Content="" Tag="Embedded"/>
                                                    <ComboBoxItem x:Name="DlgSrv_RdpModeExternal" Content="" Tag="External"/>
                                                </ComboBox>
                                                <ComboBox Grid.Row="1" Grid.Column="1"
                                                          TabIndex="61"
                                                          Margin="12,0,0,0"
                                                          SelectedValue="{Binding RdpAspectRatio}"
                                                          SelectedValuePath="Tag"
                                                          Style="{StaticResource DialogFormComboBoxStyle}">
                                                    <ComboBoxItem x:Name="DlgSrv_AspectStretch" Content="" Tag="Stretch"/>
                                                    <ComboBoxItem x:Name="DlgSrv_AspectPreserve" Content="" Tag="Preserve"/>
                                                </ComboBox>
                                                <TextBlock x:Name="DlgSrv_AudioModeLabel" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                                <TextBlock x:Name="DlgSrv_ColorDepthLabel" Grid.Row="2" Grid.Column="1" Style="{StaticResource LabelStyle}" Margin="12,14,0,4"/>
                                                <ComboBox x:Name="DlgSrv_RdpAudioModeCombo" Grid.Row="3" Grid.Column="0"
                                                          TabIndex="62"
                                                          SelectedValue="{Binding RdpAudioMode}"
                                                          SelectedValuePath="Tag"
                                                          Style="{StaticResource DialogFormComboBoxStyle}">
                                                    <ComboBoxItem x:Name="DlgSrv_AudioDisabled" Tag="0"/>
                                                    <ComboBoxItem x:Name="DlgSrv_AudioLocal" Tag="1"/>
                                                    <ComboBoxItem x:Name="DlgSrv_AudioRemote" Tag="2"/>
                                                </ComboBox>
                                                <ComboBox x:Name="DlgSrv_RdpColorDepthCombo" Grid.Row="3" Grid.Column="1"
                                                          TabIndex="63"
                                                          Margin="12,0,0,0"
                                                          SelectedValue="{Binding RdpColorDepth}"
                                                          SelectedValuePath="Tag"
                                                          Style="{StaticResource DialogFormComboBoxStyle}">
                                                    <ComboBoxItem x:Name="DlgSrv_Color16" Tag="16"/>
                                                    <ComboBoxItem x:Name="DlgSrv_Color24" Tag="24"/>
                                                    <ComboBoxItem x:Name="DlgSrv_Color32" Tag="32"/>
                                                </ComboBox>
                                                <!-- Inline validation: Audio Mode / Color Depth -->
                                                <TextBlock Grid.Row="4" Grid.Column="0"
                                                           Text="{Binding AudioModeError}"
                                                           Style="{StaticResource FieldValidationErrorStyle}"
                                                           Visibility="{Binding AudioModeError, Converter={StaticResource NullToVisibilityConverter}}"/>
                                                <TextBlock Grid.Row="4" Grid.Column="1"
                                                           Text="{Binding ColorDepthError}"
                                                           Style="{StaticResource FieldValidationErrorStyle}"
                                                           Margin="12,2,0,0"
                                                           Visibility="{Binding ColorDepthError, Converter={StaticResource NullToVisibilityConverter}}"/>
                                            </Grid>
                                            <StackPanel Margin="0,14,0,0">
                                                <CheckBox x:Name="DlgSrv_NlaCb" TabIndex="64" IsChecked="{Binding RdpNla}" Style="{StaticResource DialogFormCheckBoxStyle}"/>
                                                <CheckBox x:Name="DlgSrv_MultiMonitorCb" TabIndex="65" IsChecked="{Binding RdpMultiMonitor}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                                <CheckBox x:Name="DlgSrv_DynamicResCb" TabIndex="66" IsChecked="{Binding RdpDynamicResolution}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                                <CheckBox x:Name="DlgSrv_AudioCaptureCb" TabIndex="67" IsChecked="{Binding RdpAudioCapture}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- RDP device redirection -->
                                    <Expander x:Name="DlgSrv_DeviceRedirExpander" IsExpanded="True">
                                        <Expander.Style>
                                            <Style TargetType="Expander" BasedOn="{StaticResource ThemedExpanderStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="RDP">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Expander.Style>
                                        <StackPanel Margin="0,12,0,0">
                                            <CheckBox x:Name="DlgSrv_RedirClipboardCb" TabIndex="68" IsChecked="{Binding RedirectClipboard}" Style="{StaticResource DialogFormCheckBoxStyle}"/>
                                            <CheckBox x:Name="DlgSrv_RedirDrivesCb" TabIndex="69" IsChecked="{Binding RedirectDrives}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_RedirPrintersCb" TabIndex="70" IsChecked="{Binding RedirectPrinters}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_RedirComPortsCb" TabIndex="71" IsChecked="{Binding RdpRedirectComPorts}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_RedirSmartCardsCb" TabIndex="72" IsChecked="{Binding RdpRedirectSmartCards}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_RedirWebcamCb" TabIndex="73" IsChecked="{Binding RdpRedirectWebcam}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_RedirUsbCb" TabIndex="74" IsChecked="{Binding RdpRedirectUsb}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                        </StackPanel>
                                    </Expander>

                                    <!-- RDP advanced behavior -->
                                    <Expander x:Name="DlgSrv_AdvancedRdpExpander" IsExpanded="False">
                                        <Expander.Style>
                                            <Style TargetType="Expander" BasedOn="{StaticResource ThemedExpanderStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="RDP">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Expander.Style>
                                        <StackPanel Margin="0,12,0,0">
                                            <CheckBox x:Name="DlgSrv_GlobalDefaultsCb" TabIndex="75" IsChecked="{Binding RdpUseGlobalDefaults}" Style="{StaticResource DialogFormCheckBoxStyle}"/>
                                            <CheckBox x:Name="DlgSrv_AntiIdleCb" TabIndex="76" IsChecked="{Binding RdpAntiIdle}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_BitmapCacheCb" TabIndex="77" IsChecked="{Binding RdpBitmapCaching}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_RdpCompressionCb" TabIndex="78" IsChecked="{Binding RdpCompression}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_AutoReconnectCb" TabIndex="79" IsChecked="{Binding RdpAutoReconnect}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                        </StackPanel>
                                    </Expander>

                                    <!-- RDP experience/performance -->
                                    <Expander x:Name="DlgSrv_RdpExperienceExpander" IsExpanded="False">
                                        <Expander.Style>
                                            <Style TargetType="Expander" BasedOn="{StaticResource ThemedExpanderStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="RDP">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Expander.Style>
                                        <StackPanel Margin="0,12,0,0">
                                            <CheckBox x:Name="DlgSrv_PerfDisableWallpaperCb" TabIndex="80" Style="{StaticResource DialogFormCheckBoxStyle}"/>
                                            <CheckBox x:Name="DlgSrv_PerfDisableThemesCb" TabIndex="81" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_PerfDisableAnimationsCb" TabIndex="82" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_PerfDisableDragCb" TabIndex="83" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_PerfDisableCursorShadowCb" TabIndex="84" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_PerfEnableFontSmoothingCb" TabIndex="85" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_PerfEnableCompositionCb" TabIndex="86" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                            <CheckBox x:Name="DlgSrv_DisableUdpCb" TabIndex="87" IsChecked="{Binding RdpDisableUdp}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,12,0,0"/>
                                        </StackPanel>
                                    </Expander>

                                    <!-- SSH options -->
                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="SSH">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="SFTP">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_SshOptionsTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock x:Name="DlgSrv_SshOptionsDesc"
                                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <Grid Margin="0,16,0,0">
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ConnectionType}" Value="SSH">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Grid.Style>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="140"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock x:Name="DlgSrv_SshModeLabel" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,6,0,0"/>
                                                <ComboBox Grid.Column="1"
                                                          TabIndex="80"
                                                          SelectedValue="{Binding SshMode}"
                                                          SelectedValuePath="Tag"
                                                          Style="{StaticResource DialogFormComboBoxStyle}">
                                                    <ComboBoxItem x:Name="DlgSrv_SshModeEmbedded" Content="" Tag="Embedded"/>
                                                    <ComboBoxItem x:Name="DlgSrv_SshModeExternal" Content="" Tag="External"/>
                                                </ComboBox>
                                            </Grid>
                                            <StackPanel Margin="0,14,0,0">
                                                <CheckBox x:Name="DlgSrv_SshCompressionCb" TabIndex="81" IsChecked="{Binding SshCompression}" Style="{StaticResource DialogFormCheckBoxStyle}"/>
                                                <CheckBox x:Name="DlgSrv_SshAgentFwdCb" TabIndex="82" IsChecked="{Binding SshAgentForwarding}" Style="{StaticResource DialogFormCheckBoxStyle}" Margin="0,6,0,0"/>
                                                <CheckBox x:Name="DlgSrv_SshX11Cb"
                                                          TabIndex="83"
                                                          IsChecked="{Binding SshX11Forwarding}"
                                                          Margin="0,6,0,0">
                                                    <CheckBox.Style>
                                                        <Style TargetType="CheckBox" BasedOn="{StaticResource DialogFormCheckBoxStyle}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ConnectionType}" Value="SSH">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </CheckBox.Style>
                                                </CheckBox>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Local Shell advanced options -->
                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="Local">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_LocalShellTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock x:Name="DlgSrv_LocalShellDesc"
                                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <TextBlock x:Name="DlgSrv_WorkingDirLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                            <TextBox TabIndex="84"
                                                     Text="{Binding LocalShellWorkingDirectory, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource DialogFormTextBoxStyle}"/>
                                            <DockPanel Margin="0,12,0,0">
                                                <TextBlock x:Name="DlgSrv_ElevationModeLabel" Text=""
                                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                <ComboBox x:Name="DlgSrv_ElevationModeCmb"
                                                          TabIndex="85" Width="200"
                                                          Background="{DynamicResource CardBrush}"
                                                          Foreground="{DynamicResource TextPrimaryBrush}"
                                                          BorderBrush="{DynamicResource BorderBrush}"
                                                          VerticalAlignment="Center"/>
                                            </DockPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Citrix advanced options -->
                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="Citrix">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_CitrixTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock x:Name="DlgSrv_CitrixDesc"
                                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <TextBlock x:Name="DlgSrv_IcaFileLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                            <TextBox TabIndex="86"
                                                     Text="{Binding CitrixIcaFilePath, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource DialogFormTextBoxStyle}"/>
                                            <TextBlock x:Name="DlgSrv_CitrixHint"
                                                       Style="{StaticResource DialogHintTextStyle}" Margin="0,8,0,0"/>
                                            <CheckBox x:Name="DlgSrv_SeamlessCb"
                                                      TabIndex="87"
                                                      IsChecked="{Binding CitrixSeamlessMode}"
                                                      Foreground="{DynamicResource TextPrimaryBrush}"
                                                      Margin="0,12,0,0"/>
                                            <CheckBox x:Name="DlgSrv_SsoCb"
                                                      TabIndex="88"
                                                      IsChecked="{Binding CitrixUseSso}"
                                                      Foreground="{DynamicResource TextPrimaryBrush}"
                                                      Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- FTP options -->
                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="FTP">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_FtpOptionsTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock x:Name="DlgSrv_FtpOptionsDesc"
                                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <CheckBox x:Name="DlgSrv_FtpPassiveCb"
                                                      TabIndex="93"
                                                      IsChecked="{Binding FtpPassiveMode}"
                                                      Foreground="{DynamicResource TextPrimaryBrush}"
                                                      Margin="0,14,0,0"/>
                                            <CheckBox x:Name="DlgSrv_FtpSslCb"
                                                      TabIndex="94"
                                                      IsChecked="{Binding FtpUseSsl}"
                                                      Foreground="{DynamicResource TextPrimaryBrush}"
                                                      Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- VNC options -->
                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="VNC">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_VncOptionsTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock x:Name="DlgSrv_VncOptionsDesc"
                                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <CheckBox x:Name="DlgSrv_VncViewOnlyCb"
                                                      TabIndex="95"
                                                      IsChecked="{Binding VncViewOnly}"
                                                      Foreground="{DynamicResource TextPrimaryBrush}"
                                                      Margin="0,14,0,0"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Telnet options -->
                                    <Border>
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource DialogSectionCardStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="Telnet">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_TelnetOptionsTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock x:Name="DlgSrv_TelnetOptionsDesc"
                                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <TextBlock x:Name="DlgSrv_TelnetSecurityWarning"
                                                       Foreground="{DynamicResource WarningTextBrush}"
                                                       TextWrapping="Wrap"
                                                       Margin="0,14,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>

                        <!-- ============================================ -->
                        <!-- Tab: Info (organization + metadata)          -->
                        <!-- ============================================ -->
                        <TabItem x:Name="DlgSrv_TabInfo" Style="{DynamicResource ThemedTabItemStyle}">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel Margin="{StaticResource SpacingSm}">
                                    <Border Style="{StaticResource DialogSectionCardStyle}">
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_OrgTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock x:Name="DlgSrv_OrgDesc"
                                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <Grid Margin="0,16,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock x:Name="DlgSrv_FolderLabel" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                                                <TextBox Grid.Row="1" Grid.Column="0"
                                                         TabIndex="100"
                                                         Text="{Binding Group, UpdateSourceTrigger=PropertyChanged}"
                                                         Style="{StaticResource DialogFormTextBoxStyle}"/>
                                                <TextBlock x:Name="DlgSrv_EnvironmentLabel" Grid.Row="2" Grid.Column="0" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                                <TextBlock x:Name="DlgSrv_FavoriteLabel" Grid.Row="2" Grid.Column="1" Style="{StaticResource LabelStyle}" Margin="12,14,0,4"/>
                                                <ComboBox Grid.Row="3" Grid.Column="0"
                                                          TabIndex="101"
                                                          SelectedValue="{Binding Environment}"
                                                          SelectedValuePath="Tag"
                                                          Style="{StaticResource DialogFormComboBoxStyle}">
                                                    <ComboBoxItem x:Name="DlgSrv_EnvNone" Content="" Tag="None"/>
                                                    <ComboBoxItem x:Name="DlgSrv_EnvProduction" Content="" Tag="Production"/>
                                                    <ComboBoxItem x:Name="DlgSrv_EnvStaging" Content="" Tag="Staging"/>
                                                    <ComboBoxItem x:Name="DlgSrv_EnvLab" Content="" Tag="Lab"/>
                                                    <ComboBoxItem x:Name="DlgSrv_EnvPersonal" Content="" Tag="Personal"/>
                                                </ComboBox>
                                                <CheckBox Grid.Row="3" Grid.Column="1"
                                                          Margin="12,2,0,0"
                                                          x:Name="DlgSrv_MarkFavoriteCb"
                                                          TabIndex="102"
                                                          IsChecked="{Binding IsFavorite}"
                                                          Style="{StaticResource DialogFormCheckBoxStyle}"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <Border Style="{StaticResource DialogSectionCardStyle}">
                                        <StackPanel>
                                            <TextBlock x:Name="DlgSrv_MetadataTitle" Style="{StaticResource DialogSectionTitleStyle}"/>
                                            <TextBlock x:Name="DlgSrv_MetadataDesc"
                                                       Style="{StaticResource DialogSectionDescriptionStyle}"/>
                                            <TextBlock x:Name="DlgSrv_TagsLabel" Style="{StaticResource LabelStyle}" Margin="0,14,0,4"/>
                                            <TextBox TabIndex="103" Text="{Binding Tags, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DialogFormTextBoxStyle}"/>
                                            <TextBlock x:Name="DlgSrv_MacAddressLabel" Style="{StaticResource LabelStyle}" Margin="0,12,0,4"/>
                                            <TextBox TabIndex="104" Text="{Binding MacAddress, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DialogFormTextBoxStyle}"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>
                    </TabControl>
                </Border>

                </StackPanel> <!-- end FormFieldsPanel -->

            </StackPanel>
        </ScrollViewer>

        <!-- Validation error -->
        <TextBlock Grid.Row="1"
                   Text="{Binding ValidationError}"
                   Foreground="{DynamicResource ErrorTextBrush}"
                   FontSize="{StaticResource FontSizeBody}"
                   Margin="0,8,0,0"
                   TextWrapping="Wrap"
                   Visibility="{Binding ValidationError, Converter={StaticResource NullToVisibilityConverter}}"/>

        <!-- Action buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="DlgSrv_CancelBtn"
                    IsCancel="True"
                    MinWidth="80"
                    Margin="0,0,8,0"
                    Style="{DynamicResource SecondaryButtonStyle}"/>
            <Button x:Name="DlgSrv_SaveBtn"
                    IsDefault="True"
                    MinWidth="80"
                    Click="OnSaveClick"
                    Style="{DynamicResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
