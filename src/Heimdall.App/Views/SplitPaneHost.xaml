<UserControl x:Class="Heimdall.App.Views.SplitPaneHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="800">
    <Grid x:Name="RootGrid" ClipToBounds="True">
        <Border x:Name="PrimaryBorder" ClipToBounds="True"
                BorderThickness="0" BorderBrush="Transparent">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="BorderThickness" Value="2"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <ContentPresenter x:Name="PrimaryPresenter" ClipToBounds="True"/>
        </Border>
        <GridSplitter x:Name="Splitter"
                      Visibility="Collapsed"
                      Background="{DynamicResource CardBrush}"
                      ShowsPreview="True"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch">
            <GridSplitter.Style>
                <Style TargetType="GridSplitter">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </GridSplitter.Style>
        </GridSplitter>
        <Border x:Name="SecondaryBorder" ClipToBounds="True"
                BorderThickness="0" BorderBrush="Transparent"
                Visibility="Collapsed">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="BorderThickness" Value="2"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <ContentPresenter x:Name="SecondaryPresenter" ClipToBounds="True"/>
        </Border>
    </Grid>
</UserControl>
